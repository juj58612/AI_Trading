# 📊 策略機制分析筆記（持續更新的活文件）

> **重要**：這份文件記錄的是「目前資料庫」得出的分析結論，資料庫（實驗數量、股票池、程式邏輯）改變後，
> 這裡的具體數字都可能跟著改變。**每次要修改或精進模型架構之前，一定要先重新讀這份文件**，並在做完
> 新的分析或改動後，回來更新對應章節（新增一個帶日期的小節，不要直接覆蓋舊結論——保留演變過程）。
>
> 舊版分析報告 `big_data_attribution_report.md`（2026-08-01，277組實驗、42,104筆交易）內容已過時
> （當時股票池、部分策略邏輯與現在不同），與本文件衝突時**以本文件為準**。

---

## 2026-08-06 分析：為什麼排行榜「排序一是方案A、其他好結果多是方案D或B」

### 資料集基準
- 504 組實驗（6種持倉檔數 × 4種出場方案 × 14種持倉天數 × 3種微幅日期變化組合）
- 70 檔股票池，資料涵蓋 2021-01-04 ~ 2026-08-04+
- 出場方案定義（見 `strategy_core.py` 的 `evaluate_exit()`）：
  - **方案A（積分反轉）**：跌破5MA 且 籌碼轉弱，需連續2天同時成立才出場
  - **方案B（土洋雙賣）**：外資與投信同一天都賣超，當天立即出場
  - **方案C（動態防線收縮）**：外資或投信任一方轉弱（連續2天），ATR停損乘數收緊至1.0x
  - **方案D（自適應吊燈鎖利）**：ATR停損乘數1.5x（獲利>12%後收緊至1.25x），**唯一沒有15%固定停利上限的方案**
  - 四個方案共用：ATR吊燈移動停損、15%固定停利（D除外）、持倉天數上限強制出場

### 核心發現：這不是「哪個方案比較強」，是兩個維度的交互作用

**現象**：#1（總報酬333.51%）是方案A（3檔/360天）；但排行榜前30名裡，方案D佔20筆、B佔9筆、A只有這1筆、C是0筆。

**根因1：504組整體平均，方案B其實才是最強**（不是A也不是D）

| 方案 | 504組平均報酬率 | 平均MDD | 平均勝率 | 平均獲利因子 | 單組最高 |
|---|---|---|---|---|---|
| B | 158.8% | 47.4% | 43.4% | 1.42 | 285.8% |
| D | 150.0% | 57.1% | 43.8% | 1.41 | 327.1% |
| A | 144.9% | 55.7% | 40.5% | 1.37 | **333.5%（#1）** |
| C | 108.3% | 56.5% | 43.9% | 1.27 | 183.1% |

A的「單組最高」全場第一，但「504組平均」是四個裡第二差的——A不是全面性強，只在特定角落極端突出。

**根因2：持倉天數效果，A和D的曲線形狀完全相反**

| 持倉天數 | 方案A平均報酬 | 方案D平均報酬 |
|---|---|---|
| 15天 | 136.0% | 203.2% |
| 20天 | 163.7% | **211.9%（D最高峰）** |
| 30天 | 161.1% | 183.7% |
| 60天 | 183.2% | 168.4% |
| 90天 | 179.5% | 171.5% |
| 360天 | **193.3%（A最高峰）** | 171.5%（打平） |

D在15~20天見頂後逐漸下滑打平；A則是天數越長越好，在360天（近乎無上限）達到自己的最高峰。**#1（3檔/360天）剛好命中A唯一的甜蜜點**，而排行榜其他名次的天數多落在D的主場範圍。

**根因3：持倉檔數效果，D靠分散、A怕過度分散**

| 持倉檔數 | 方案A | 方案B | 方案D |
|---|---|---|---|
| 1檔 | 93.3% | 82.4% | 48.4% |
| 5檔 | **170.5%（A高峰）** | **186.7%（B高峰）** | 183.6% |
| 10檔 | 144.1%（衰退） | 163.8% | **228.8%（D高峰，近5倍於1檔）** |

**根因4：出場原因經濟性——這是機制層面的「why」**

用全部504組的所有交易（跨實驗聚合，統計上比單一實驗更可靠）：

| 方案 | 主要出場原因 | 佔比 | 平均損益 |
|---|---|---|---|
| A | 積分轉負（自己的訊號，需連2天確認） | 66% | -2.79%（可控小賠） |
| A | 15%停利 | 18% | +17.13% |
| B | 土洋雙賣（單日觸發，最敏感） | 63% | **-1.24%（四方案最小虧損）** |
| B | 15%停利 | 15% | **+17.33%（四方案最高）** |
| D | 觸發停損（唯一出場機制） | 81% | +0.53%（幾乎打平） |
| D | **完全沒有15%停利（程式碼設計上停用）** | 0% | — |
| C | 觸發停損 | 73% | -3.22%（四方案最大虧損） |

**結論**：B贏在「快狠準」（單日觸發+仍保留15%停利，兩邊優點都占）；A贏在「嚴格篩選」（雙條件AND，很少提早出場，天數上限只是保險不是常態路徑）；D的「不設限讓獲利奔跑」設計理想沒有被吊燈停損機制兌現（平均每筆只賺0.53%），實際上是靠「檔數夠多、天數夠短」的大數法則在賺錢；C最弱，因為它繼承了D的動態收縮機制但觸發門檻更寬鬆，防守機制本身變成最大漏財出口。

### 多空年份表現（防守 vs 進攻）

| 方案 | 2022空頭年 | 2023多頭年 |
|---|---|---|
| B | **-32.9%（防守最強）** | 42.5%（進攻最弱） |
| D | -41.9%（防守最弱） | **77.9%（進攻最強）** |
| A | -40.6% | 55.7% |
| C | -39.5% | 87.9%（⚠️ C整體表現弱，此數字樣本可能不穩，需再驗證） |

### 條件式建議表（依操作條件推薦方案，取代「單一最佳解」）

| 你的狀況 | 建議方案 |
|---|---|
| 短線快進快出（2-15天） | 方案D |
| 一般波段操作（20-60天） | 方案B（其次D，D在15~20天有單點極值） |
| 長線持有、不常盯盤 | 方案B（其次A，A僅在近乎無上限天數才突出） |
| 預期空頭/高波動 | 方案B（防守最強） |
| 預期強勢多頭 | 方案D（進攻最強） |
| 重壓少數幾檔（1-3檔） | 方案A或B |
| 大分散布局（8-10檔） | 方案D（優勢最明顯） |

### 方法論警示（避免未來誤用這些結論）
1. **以上全部是同一份歷史資料（2021-2026）的樣本內（in-sample）結果**，尚未做訓練/測試集切分驗證，不能排除對這段特定歷史過度配適的風險。
2. C在2023年的87.9%可能是小樣本雜訊，不宜直接採信為「C進攻力最強」。
3. 「持倉天數區間」有兩種算法會給出不同答案：逐一天數精算峰值（如上表）vs 區間分桶平均（短線/波段/長線分桶），兩者方法論不同，解讀時要注意用的是哪一種。

### 後續討論串（下次要接續的方向）
- [ ] `get_attribution()`（`backtest_engine.py`）要重寫成動態依方案交叉分析，不要再寫死比較D跟C
- [x] Tier 3（逐日NAV／Sharpe/Sortino）討論：回測引擎其實已經在算逐日equity（用來算MDD），只是算完就丟掉沒存資料庫。不用額外花錢買資料，改動是把已經在算的東西存下來+重跑504組。（已討論，尚未實作）

## 2026-08-06（續）：方案E 第一輪嘗試 — 兩版都失敗，但失敗得有意義

**設計動機**：使用者質疑「條件式推薦 A/B/C/D」是不是在迴避尋找真正更好的方案，主張「若設計出的新方案通過驗證證明有效，就是真的進步」。這個立場是對的——先前「沒有永遠最佳解」的說法，準確意思應該是「不要迷信單一歷史數據裡的某一格是永恆真理」，不是「不要嘗試設計更好的方案」。

**v1 設計**：借用方案B的「土洋雙賣」單日觸發（實測平均虧損四方案最小），但加一個過濾條件：只在部位「目前未實現虧損」時才觸發；若雙賣當天部位還在獲利，忽略訊號、續抱。
**v1 結果（5檔/20天）**：總報酬 130.85%，**遠低於同組合的 D(303.7%)／A(197.8%)／B(184.6%)／C(140.4%)**。

**v1 診斷（第一次比較方法有誤，已修正）**：一開始誤判「E的虧損分支平均每筆虧 -8,223元，比B整體平均 -3,206元差很多」，但這是**混淆的比較**——B的-3,206元是「觸發時還在賺」與「觸發時已經賠」兩種情況混在一起平均出來的。拆開重算：B單獨看「觸發時已經賠」的那個子集，平均是 -9,076元，跟E的-8,223元其實**沒有顯著差異**。真正的問題不在這個分支。

**v2 設計**：既然v1的虧損分支沒問題，改良「獲利中忽略訊號」這部分——不要整個忽略，改成「收緊移動停損到接近現價」（比照C/D的動態收縮邏輯），先鎖住獲利、但留一點空間讓它繼續漲。
**v2 結果（5檔/20天）**：總報酬 **106.78%，比v1更差**。新增的「雙賣後鎖利出場」分支只觸發11次、平均+3,390元，貢獻有限；核心問題是「不像B那樣在雙賣當天立即獲利了結」，導致部位曝險時間拉長，最終反而在其他出場路徑（觸發停損等）付出更差的代價。

**擴大測試（8組持倉檔數/天數組合）**：E在3~5檔幾乎每次墊底；在8~10檔躍升到第2名（僅次於D），但即使在這個最佳角落也還是輸D一大截（196 vs 278、207 vs 286）。

**誠實結論**：
1. 原始假設（「B在獲利部位上被雙賣訊號提早洗出場，浪費了2023多頭年的上漲空間」）**沒有被數據支持**——B「不管賺賠、雙賣當天就砍」這條規則本身已經調得相當好，不是明顯缺陷。
2. E目前在任何測試過的組合都沒有贏過現有最佳選項（通常是D）。
3. **沒有繼續用手動猜測微調第三版**——避免在同一份歷史資料上反覆調整湊出「看起來變好」的版本，這正是過度配適的典型陷阱。
4. 程式碼（`strategy_core.py` 的 `strategy == 'E'` 分支）與 UI（`backtest.html` 單次回測下拉選單、Mega Grid 勾選框，皆標示「🧪實驗性」）已經留著，之後如果要做更系統化的搜尋（見下方Optuna建議）可以直接沿用這個掛載點，不用重新接線。

### 下次要接續的方向
- [ ] **改用系統化參數搜尋，不要再手動猜**：本系統已經有 Optuna 貝氏最佳化的基礎設施（AI 網格最佳化功能，已內建 70/30 訓練/測試切分）。與其手動設計「方案E」的規則再一次次回測，應該把出場邏輯的關鍵參數（ATR乘數、停利百分比、確認天數、觸發門檻）變成連續可調的變數，交給 Optuna 在訓練集上搜尋，並在測試集（out-of-sample）驗證，才能真正說服人這不是巧合。
- [ ] `get_attribution()` 動態化這件事還沒做，見上方。
- [ ] Tier 3 逐日NAV還沒實作，見上方。
- [ ] **選股因子模型（新方向，2026-08-06 提出，範疇已確認）**：目前深度掃描選股只看「站上5MA + 外資投信買賣超」這個很陽春的單一籌碼因子。使用者確認**先只用現有免費資料深化，不加基本面**（本益比/EPS成長率這類基本面因子通常要花錢買資料源，之後真的需要再另外評估）。方向：用現有504+組回測結果 + 技術面（均線排列、ATR波動度、乖離率）+ 籌碼面（外資投信買賣超、FinMind免費額度內的融資融券）去找出更細緻的客觀選股因子組合，用於深度掃描後的買賣推薦，不需要新增付費資料源。尚未開始設計，下次接續時從這裡開始。
- [ ] **首頁持股主動提醒（新發現，2026-08-06）**：目前系統「檢查持股該不該買賣」的邏輯只存在 `order_planner.html`（下單規劃頁），且只有使用者真的點進那頁才會計算；首頁 `index.html` 不會主動提醒。使用者可能想要在首頁加提示（例如「有N筆持倉觸發停損/停利，點此查看」），尚未決定是否要做，下次可以直接問。

## 2026-08-06（續2）：打通「回測驗證出的出場邏輯」與「實戰持倉賣出提醒」

**問題**：`history.html` 持股卡片上的紅色警示框一直是寫死的簡單規則（固定±8%停損、或手動設定的支撐/壓力價），跟今天整輪分析驗證出來的 `strategy_core.evaluate_exit()` 完全脫節——選好方案A/B/C/D、跑完504組回測，結果實戰持倉根本沒在用這套邏輯。使用者認定這是核心缺口，一定要打通。

**實作**：新增 `GET /api/portfolio/sell_check`（`main.py`）：
- 對每筆多單持倉，即時用 `fetch_yfinance_history(ticker, period="2y")` 抓兩年價格 + `fetch_chip_data_from_finmind(ticker, lookback_days=...)` 抓對應長度籌碼資料（兩個函式都改成可傳參數，預設值不變、不影響既有呼叫點）。
- 用 `strategy_core.calculate_indicators()` 算出 MA5/MA20/ATR（跟回測引擎共用同一套技術指標公式），從 `buy_date` 隔天開始逐日重播 `strategy_core.evaluate_exit()`，一路推進 `highest_price`/`trailing_stop` 到今天，找出「第一次觸發賣出的日期與原因」。
- 空單（`type: 'short'`）暫不支援（evaluate_exit 的吊燈邏輯是多單假設），回傳 error 訊息略過。
- 商業邏輯決定：持倉沒有 `exit_strategy` 欄位時**預設用方案D**（不強迫使用者選），沒有 `max_hold_days` 時**預設視為無限制(999天)**，兩者都會在回傳的 `notes` 裡明講「這是預設值不是使用者指定」，避免使用者誤以為系統真的知道他要哪個方案。這是刻意選擇「系統給合理預設 + 透明告知」而不是「強迫使用者建倉時就要選」，因為目前建倉流程(`commit_planner_orders`) 還沒有 UI 讓使用者選方案，只是程式碼裡加了欄位鋪路。

**UI**：`history.html`/`history.js`「現役部隊」區塊最上方加橫幅（🚨 N 檔觸發 / ✅ 目前無觸發），列出觸發原因+日期，可點擊捲動到對應卡片；每張持股卡片新增「🧭 模型驗證訊號」區塊，跟原本寫死規則的警示框並存顯示、不互相取代，明確標示兩者是不同機制。

**已驗證**：本機測試用同一筆 2330 持倉分別套 A/B/C/D 四種方案重播，觸發原因/日期/trailing_stop 都符合預期（B在土洋雙賣觸發最早、D/C走觸發停損路徑），瀏覽器手動測試橫幅與卡片皆正確渲染。剛買進當天（沒有隔天資料可比對）會正確顯示「才剛買進，尚無新交易日資料可比對」而不是誤判。

**尚未做**：`order_planner.html` 的 `trailing_stop` 計算（下單當下算一次、用粗略 ATR 估算、之後不再更新）沒有改用這套邏輯，兩邊目前是分開的兩條路徑，之後可以考慮統一。

## 2026-08-07：下單規劃器加「跳空/ATR比」警示

**問題**：`order_planner.html` 的買進建議 `price` 是掃描當下（通常是前一天收盤）算出來的推薦價，使用者實際下單是隔一個交易日，這中間如果開盤跳空，用推薦價去追價，等於用一個回測從沒驗證過、而且離移動停損很近的價位進場。使用者問「這種情況該怎麼決策」，討論出的原則是拿跳空幅度跟 ATR 比：0.5倍以內是正常雜訊、0.5~1倍提醒留意、1倍以上明確不建議追價（除非有明確籌碼/消息面支撐這個跳空）。

**實作**：
- `run_scan()`（`main.py`）的 `process_ticker` 現在會多算一個 `atr` 欄位（用既有的 `calc_atr(hist)`），跟著流進 `daily_scan_cache.json`/`latest_scan_results.json`，`potential_buys`/`buys`/`filtered_buys` 都一路帶著這個欄位往下傳。
- `/api/planner/recommendations` 在組好最終 `buys` 清單後，額外對每一檔用**不同的 cache_key**（`period="5d"` 而不是掃描用的 `period="1mo"`）重新抓一次即時報價，避免拿到跟掃描當下同一份快取、算出跳空永遠是 0 的假結果。算出 `live_price`／`gap_amount`／`gap_atr_ratio` 一併回傳。
- 前端 `order_planner.js` 新增 `buildGapWarningHtml()`：只在**向上跳空**（追高風險）且比例 ≥ 0.5 倍 ATR 時才顯示，向下跳空（等於用比推薦價更便宜的成本進場）不視為風險、不顯示警示。0.5~1倍黃底提醒、≥1倍紅底明確警示，用跟 `history.html` 賣出訊號同一套 `status-banner`（已搬到共用 `style.css`，兩頁共用同一套視覺語言）。
- **已驗證**：本機用假資料測過 0.1x（不顯示）、0.7x（黃色）、1.4x（紅色）、負值下跳空（不顯示）、ATR缺值（不顯示）五種情境，邏輯符合預期；也實際重新掃描過全股池 70 檔，確認 `atr` 欄位會正確寫進快取，不再是 0。

**尚未做**：目前只對最終入選的 `buys`（≤5檔）即時查即時價，`filtered_buys`（備份標的）沒有算跳空，因為使用者通常只會臨時手動下單這些備份標的、不是主線流程，先不做以控制額外的 yfinance 呼叫量。

## 2026-08-07（續2）：規劃「買入持有回測計算機」（獨立於 A/B/C/D 模型之外）— 頁面與後端已完成，等股票清單

**動機**：使用者要一個完全不套用 `evaluate_entry`/`evaluate_exit`、不需要任何選股/出場模型的純算術工具——選一檔股票、一段期間、投入多少股或多少錢，中途不賣，直接算最終報酬。例如「0050 從 2021.1.1 買到 2026.1.1，中間不賣，賺多少？」跟現有「大數據回測」那套 A/B/C/D 進出場邏輯是完全不同的兩件事，不要混在一起。

**UI 架構決定**：`backtest.html` 現有編號 1~6 依此往後遞增，新插入一個「2. 買入持有回測」區塊：
- 原「2. 單次回測」→ 改名為「3. 單次回測（套用模型策略）」，明確跟新區塊的「不套用模型」區隔開來，避免使用者搞混兩者算的是不同東西。
- 「3. AI 網格最佳化」「4. 大數據回測」「5. 個股買賣交易明細」「6. 排行榜」依序遞增為 4/5/6/7。
- 新的「2. 買入持有回測」區塊內容：
  - 小型「同步歷史資料庫」子區塊（只針對這個新股池，跟現有 70 檔 AI 股同步分開，見下方資料庫決定）
  - 選股（可複選）+ 時間範圍
  - 買入數量/金額雙向智慧換算：填股數自動算要多少錢、填預算自動算買得起幾張，沿用 `order_planner.js` 裡 `calcManualShares()` 已經在用的同一套換算模式，體驗一致，不用重新發明
  - 結果直接列在同一區塊內（不跳頁），複選多檔時逐檔列出比較

**已拍板的技術決定**（使用者 2026-08-07 直接回覆確認）：
1. **新股池同步只抓股價，跳過 FinMind 法人籌碼**——買入持有計算機用不到籌碼，跳過可以大幅加速同步（現有 70 檔慢就是慢在籌碼那段，每檔要等 1 秒防鎖），也不用煩惱 ETF 有沒有籌碼資料這個不確定因素。**代價**：以後如果想拿這些 ETF/個股去跑 A/B/C/D 模型回測，籌碼資料要另外補抓。
2. **獨立成新的資料庫 JSON 檔案**，不併入現有 `backtest_database.json`——兩個股票池（AI選股模型池 vs 任意股票買入持有池）語意上完全分開，以後互相增減股票清單不會干擾對方的回測基準。檔名待定（暫定 `backtest_database_custom.json`，實作時再跟使用者確認）。
3. **含息計算**——0050 這類長期配息 ETF，只算價差會嚴重低估實際報酬，要抓配息紀錄一併算進總報酬。
4. **複選多檔比較時，統一套用同一組時間範圍/投入金額**（不是每檔各自獨立設定），確保是同條件下的公平比較（apples-to-apples）。

**5. 手續費/證交稅**：計入（`FEE_RATE=0.1425%` 買賣皆收、`TAX_RATE=0.3%` 賣出時收，跟主系統下單規劃器假設一致），但前端有「計入／不計入」下拉切換，使用者可以自己選要看淨報酬還是純市場報酬。
**6. 股票選取 UI**：下拉式選單，使用者從股池勾選後按「確定」（不是常駐展開的一大排 checkbox）。

**2026-08-07 已完成實作**：
- 後端（`backtest_engine.py`）：`CUSTOM_DB_PATH`（`backtest_database_custom.json`，檔名已定案）+ `CUSTOM_LIST_PATH`（`custom_stock_list.txt`，一行一檔跟 `ai_stock_list.txt` 同款式）。新增 6 個 endpoint：`GET /api/buyhold/pool`、`POST /api/buyhold/pool/add`、`POST /api/buyhold/pool/remove`、`GET /api/buyhold/status`、`POST /api/buyhold/sync`、`POST /api/buyhold/calculate`。
  - 同步用 `yf.Ticker(t+suffix).history(auto_adjust=False, actions=True)`，`.TW`/`.TWO` 兩個都試一次（不用像 AI 股池那樣手動維護 OTC 清單）；只存 `close` 跟 `dividend` 兩欄，斷點續傳邏輯跟主資料庫一致（已有資料只補最後一天之後）。
  - **配息資料來源決定**：用 `auto_adjust=False, actions=True` 抓原始收盤價 + 個別的 `Dividends` 欄位分開存，**不是**用 `auto_adjust=True` 的還原股價（那樣會把配息隱含在價格裡，沒辦法在結果拆開顯示「價差」vs「配息」兩個數字）。結果裡 `end_market_value`（價差部分）跟 `total_dividends`（配息現金，不假設再投入）是分開欄位，使用者可以自己看兩者占比。
  - 已驗證 yfinance 的 `Close` 欄位本身就會做**股票分割還原**（即使 `auto_adjust=False`），只有配息不還原，所以價格不會因為除權息分割出現不連續的假斷崖，不用自己額外處理分割比例。
  - `calculate` 支援複選多檔＋錯誤隔離（某檔沒同步過只會該列顯示錯誤訊息，不會讓整批請求失敗）。
- 前端：`backtest.html` 新增「2. 買入持有回測」區塊（原 2~6 依序遞增為 3~7，含 `megaSharedConditionsText` 裡的文字引用也一併更新），新檔案 `buyhold.js`（沿用 `backtest.js` 已宣告的 `BACKTEST_API_URL`/`IS_LOCAL`，不重複宣告）。已加入 `main.py` 靜態白名單。UI 包含：股池管理（新增/移除代號的標籤列）、下拉式複選股票選單（勾選+確定按鈕）、投入方式切換（金額/股數）、日期選擇（沿用單次回測的年/月/日下拉樣式）、結果表格（不跳頁，直接列在同一區塊內）。
- **已驗證**：本機用 0050 實測全流程（新增股池→同步→勾選→計算），2021-01-04~2025-12-31、投入100萬、含手續費：期末市值 209.8萬 + 累積配息 17.1萬 = 總值 226.9萬，總報酬 126.87%、年化 17.85%；不含手續費版本、雙檔（0050+未同步的2330）錯誤隔離都測過。測試完已把 0050 從股池移除、恢復股池為空，符合使用者要求的「暫時把股池為空的」狀態。

**2026-08-09 UI 精修**（架構討論收尾後，使用者直接下的具體調整指令）：
1. 「1. 同步歷史資料庫」→「1. 同步歷史資料庫（套用模型策略）」，「2. 買入持有回測」→「2. 買入持有回測（不套用模型策略）」——先各自加註記，**接著使用者又要求把兩區塊順序對調**：買入持有回測改成排在最前面變成「1.」，AI 股池同步退到「2.」，`opSection1`/`opSection2` 的 id 也跟著對調（`opSection1` 現在對應買入持有回測、`opSection2` 對應 AI 股池同步），避免 id 跟畫面編號對不上。
2. **手續費/證交稅不再提供開關**，一律計入——`BuyHoldRequest` 拿掉 `include_fees` 欄位，`calculate_buyhold()` 直接寫死套用 `BUYHOLD_FEE_RATE`/`BUYHOLD_TAX_RATE`。
3. **拿掉「投入方式」下拉選單，改成張數／總價兩欄常駐雙向試算**：新增 `GET /api/buyhold/price_on_date?ticker=&date=` 給前端查「所選第一檔股票在買入日的收盤價」，前端用這個價格即時互算——填張數自動算總價；填總價則無條件捨去算出「買得起的整張數」，並把總價欄位改寫成那個整張數的實際費用（自動刪去湊不滿一張的零頭）。只接受整張、不接受零股。複選多檔時，張數對每檔一致，但各檔實際金額依自己的股價分別計算（試算預覽用第一檔的價格，正式結果以計算後的表格為準）。`BuyHoldRequest.shares` 改成必填（前端一律換算成整張對應的股數後才送出，`amount` 欄位整個移除）。
4. 「結算日期（End Date，不賣出，僅估算此日市值）」→「結算日期（賣出獲利的數值）」——純文案調整，計算邏輯本來就已經是「模擬在這天賣出、扣手續費證交稅」，新文案更準確反映實際行為。
5. 「➕ 加入股池」→「➕ 加入查詢」——使用者說之後股池清單會是固定的（等下面的股票清單搜尋結果定案），這顆按鈕語意上比較像是「加入查詢」不是持久建池，純文案調整。
- **已驗證**：本機重測過張數→總價（87張 = 2,708,467元，跟後端 `/api/buyhold/calculate` 算出來的 buy_cost 完全一致）、總價→張數無條件捨去（100萬預算在 0050 上算出 32 張、實際花費 996,218 元、多餘 3,782 元不用），以及新的 `price_on_date` endpoint 本身。測完一樣把測試用的 0050 移除、股池恢復空的。

**2026-08-09（續）：股池改成純固定名單，不開放使用者自行增減**——使用者馬上又推翻了上面第5點的「加入查詢」文案調整，明確要求「股池是我們固定好的名單，不能選只能看」：
- **拿掉整個「股池管理」區塊**（新增輸入框＋「加入查詢」按鈕＋標籤列），使用者從此無法自行新增/移除股票，只能從「選擇要測試的股票（可複選）」下拉勾選清單裡選（清單內容 = `custom_stock_list.txt` 的固定內容，之後由使用者跟 Claude 討論後直接編輯這個檔案決定）。
- 後端拿掉 `POST /api/buyhold/pool/add`、`POST /api/buyhold/pool/remove`、`save_custom_stock_list()`、`PoolTickerRequest`，`GET /api/buyhold/pool` 保留（前端還是要讀清單來畫勾選框）。
- **新增 `GET /api/buyhold/pool_prices?start_date=&end_date=`**：勾選畫面本身就要顯示每檔的「買入時股價／賣出時股價」，不用等按下計算才知道——這支 endpoint 一次回傳股池內所有股票在買入日/結算日（或之後最近一個有資料的交易日）的收盤價。前端下拉選單打開時、或買入/結算日期變更時都會重新查詢，直接顯示在每個勾選框旁邊（例：`0050　買:31.09 / 賣:66.95`）。
- 使用者順帶問了一個方法論問題：「獲利指標單純用股價漲跌扣相關費用即可，適當嗎？」——已回覆：這對買入持有計算機本身（算「投資報酬率」）完全正確，就是目前 `/api/buyhold/calculate` 在做的事；但如果是指下面研究清單裡的 EPS/ROE/毛利率，那是另一個概念（公司基本面獲利能力，用來判斷股價上漲有沒有基本面支撐），兩者用途不同、不能互相取代，兩者都保留。
- **已驗證**：本機重測 `pool_prices` endpoint 本身，以及前端下拉選單勾選框正確顯示買/賣價格；確認「➕ 加入查詢」按鈕與相關函式已完全移除、無 console 錯誤。測完把測試用的 0050 從 `custom_stock_list.txt` 移除，恢復空清單。

**尚未決定/待辦**：
- [x] ~~股票清單還沒定~~ **2026-08-09 已定案並寫入股池，見下方完整記錄**。
- [x] ~~完成後要補進策略白皮書~~ **2026-08-09 已完成全面校對，見下方「2026-08-09（續5）」**。
- [ ] 目前只在本機測試過（`backtest_engine.py` standalone port 58889），還沒 commit/push（使用者 2026-08-09 明確指示：以後都等到不再修改程式時才 commit，見 `feedback_commit_timing` 記憶）。
- [x] ~~新增的 56 檔股票尚未實際同步歷史資料~~ **已完成，見下方「2026-08-09（續4）」**。

## 2026-08-09（續3）：股票清單定案 — 股池從 70 檔擴充到 126 檔

**過程**：使用者先要求搜尋台股四年最佳績效四十家，中途喊停（"你目前先不要去搜尋，因為我會給你範圍"），改由使用者給出明確的六大類/十三小類 AI 硬體供應鏈框架（晶片與IC設計／晶圓製造與先進封裝／關鍵零組件與上游原料／伺服器與硬體設備／散熱與電力能源／軟體與端側應用），要求 Claude 依此框架找出約150檔候選（含既有70檔）。Claude 分兩輪找出候選，每一輪都用 yfinance 交叉驗證公司名稱是否對得上代號，過程中抓到並剔除十幾個記錯的代號（例如誤把 2380 虹光精密〈掃描器〉當成雲端服務商、3483 順達科技〈通路商〉當成散熱模組廠）。最終確認 **56 檔新增候選**，加上既有 70 檔 = **126 檔**，已知會使用者確認後直接寫入所有相關檔案。

**這 56 檔新增候選的完整分類清單**，見 [AI_SUPPLY_CHAIN_NEW_CANDIDATES.md](AI_SUPPLY_CHAIN_NEW_CANDIDATES.md)（已傳給使用者）。ETF 前40名報酬排序見 [ETF_TOP40_CANDIDATES.md](ETF_TOP40_CANDIDATES.md)（ETF 只做排序、不分類、不列入股票池，僅供參考）。

### 追溯記錄：哪些是原有 70 檔，哪些是 2026-08-09 新增的 56 檔

**原有 70 檔**（2026-08-05 及之前就已存在於 `ai_stock_list.txt` / `backtest_engine.py` 的 `TICKERS`）：
```
3013 3693 3034 3711 2421 2303 6117 3189 3583 3017 6531 2360 6669 6643 2330 3035 3529 6187 6230 2356
2395 2382 2368 2313 2357 2376 8210 3661 5443 3533 2059 2383 3338 3706 3131 5269 6515 2324 3037 6414
2317 8046 2449 3044 2353 2454 3231 6282 8299 4966 2345 8996 6274 3443 3653 6213 2308 2359 6139 3324
3081 3105 2455 3163 3363 6442 3380 6830 3587 3289
```

**2026-08-09 新增 56 檔**（依六大類分類，完整分類版見上方連結的 md 檔）：
```
1503 1513 1514 1519 1560 1609 2301 2337 2355 2385 2404 2412 2436 2458 2480 2492 3005 3413 3532 4755
4958 5388 5434 6166 6183 6196 6202 6206 6214 6239 6257 6269 6285 6412 6415 6438 6533 6719 6770 8081
8114 3227 3374 3438 3680 4979 4991 5227 5347 5483 6182 6223 6488 6510 8050 8086
```

**已寫入的檔案**（2026-08-09）：
- `ai_stock_list.txt`：70→126 檔（`main.py` 首頁掃描/選股用的清單）
- `backtest_engine.py` 的 `TICKERS`：70→126 檔，並在陣列裡用 Python 註解標記了新增的分界點；`OTC_TICKERS` 也同步新增了 15 檔驗證後確認為上櫃(.TWO)的代號（3227,3374,3438,3680,4979,4991,5227,5347,5483,6182,6223,6488,6510,8050,8086）
- `custom_stock_list.txt`：從空清單直接設為跟 `ai_stock_list.txt` 完全相同的 126 檔（買入持有回測股池，使用者要求要包含原有70檔+新增的一起放入）

**已驗證**：重啟 `backtest_engine.py`/`main.py` 後，`/api/backtest/status` 正確顯示 126 檔中 70 檔已覆蓋、56 檔待同步；`/api/buyhold/status` 正確顯示 126 檔全部待同步（這個資料庫之前只測試同步過 0050，跟這 126 檔沒有交集）。兩個檔案都無語法錯誤、成功載入。

**目前狀態**：股票清單已定案並寫入所有股池檔案，後續進展見下方 2026-08-09（續4）（續5）章節。

## 2026-08-09（續4）：修正 FinMind token 遺漏 bug + 股池同步 + 納入 ETF

**發現的真實 bug**：觸發「2. 同步歷史資料庫（套用模型策略）」幫 56 檔新股票補籌碼資料時，全部靜默失敗（`covered` 卡在 70/126 不動）。追查發現 `backtest_engine.py` 的 `/api/backtest/download` 呼叫 FinMind API 時**完全沒有帶 `token` 參數**（跟 `main.py` 的 `fetch_chip_data_from_finmind` 不一樣，那邊有正確帶 token）。沒有 token 等於匿名請求，FinMind 配額極低，很快就回 `402 Requests reach the upper limit`，程式碼原本又是「非 success 就靜默跳過、不印任何錯誤」，導致完全看不出發生什麼事。

**修正**：
1. `backtest_engine.py` 的 FinMind 籌碼下載加回 `&token={finmind_token}`（用 `os.getenv("FINMIND_API_TOKEN", "")`，跟 main.py 同一套）。
2. 原本失敗時完全靜默的 `except: pass`，補上 `else: print(...)` 印出失敗原因，以後同樣的狀況會直接在伺服器 log 看到，不用像這次一樣得手動 debug。
3. 除錯過程中也發現：本機用 `nohup python3 -m uvicorn backtest_engine:app ...` 重啟時如果沒有先 `source .env`，`FINMIND_API_TOKEN` 會是空字串——這是操作上要記住的細節，不是程式碼問題（main.py 的重啟習慣一直都有 source .env，只有這次重啟 backtest_engine.py 忘記做）。

修正後重跑，**126/126 全部同步成功**（股價+籌碼都有）。買入持有股池（`custom_stock_list.txt`）也同步了 126 檔，全數成功。

**新增：買入持有股池納入 ETF**。使用者要求把之前查到的「台股 ETF 四年報酬前40名」也直接列入買入持有回測的股池（跟AI概念股的126檔放在同一個股池，但不進AI模型的 `ai_stock_list.txt`／`TICKERS`，因為ETF是一籃子股票，套用個股籌碼流向模型的邏輯沒有意義；買入持有計算機本身是純算術、不需要模型，剛好適合ETF）。40檔ETF（已排除反向型）加進 `custom_stock_list.txt`，同步成功，買入持有股池變成 **166 檔（126 AI股 + 40 ETF）**。`ai_stock_list.txt`／`TICKERS`（AI模型專用）維持 126 檔不變。

**新增 `stock_metadata.json`**：ticker → {name_cn, category, subcategory} 的完整對照表，涵蓋全部166檔（126檔AI股六大類分類，40檔ETF統一歸為「七、ETF」類）。原70檔裡有19檔先前沒分類過（不在56檔新增名單裡也不在第一輪驗證的68檔重疊名單裡），這次也一併依既有知識補上分類（例如2360致茂電子→IC封測、5269祥碩科技→高速傳輸IC、6643 M31→矽智財IP等）。

**買入持有回測下拉選單全面重做**（使用者要求「依性質分類、要有中文名稱、盡量橫式排列」）：
- 拿掉了原本「➕加入查詢」新增股票的輸入框（使用者確認股池是固定名單，使用者不能自己加，只能勾選）。
- `GET /api/buyhold/pool` 回應改成同時帶 `items`（含 name_cn/category/subcategory），前端讀 `stock_metadata.json` 分組渲染。
- 下拉選單用 CSS `columns` 多欄橫式排列（`.buyhold-cat-items { columns: 220px auto; }`），每個大類/小類一個群組區塊，群組內橫式自動換欄，避免166檔擠成一條長長的直式清單。
- 選單標題文字更新為「選擇要測試的股票（可複選，股池為AI概念股及ETF固定名單）」。
- **已驗證**：166檔全部正確分組、顯示中文名稱、多欄排列，本機截圖確認畫面正常，166/166 同步狀態正確顯示。

**遵循新原則**（使用者同一輪明確交代）：
- yfinance 現價查詢一律一天只打一次、快取在本機，不要每次查詢都即時打——已存成 `feedback_yfinance_rate_limit` 記憶。順帶修正了下單規劃器跳空警示原本用 1小時TTL快取的做法，改成 `fetch_gap_check_price()` 真正的「一天一次」快取。
- commit 時機：以後都等到整批修改真的告一段落、不會再改了，才 commit，不要中途改一半就先commit——已存成 `feedback_commit_timing` 記憶。

## 2026-08-09（續5）：策略白皮書 (doc.html) 全面校對，修正跟現況不符的敘述

使用者要求「仔細檢視量化波段策略白皮書...對照我們現在架構與條件，整理以符合此站台的架構及原理，以免誤導」。全文校對後修正的項目：

1. **1.1 節標題與內文**：「70 檔」→「126 檔」，內文補充六大類供應鏈說明，並加註「股池清單會隨產業版圖擴充持續增補，實際最新檔數請見回測實驗室頁面」，避免以後股池再擴充又要手動改白皮書數字。
2. **版本資訊 meta-box**：「2021/01~2026/07，共1,351個交易日」這種寫死的資料庫日期範圍，改成引導使用者去看回測實驗室頁面上的即時數字，不再寫死（跟第五章「不列會過時的具體數字」的既有原則一致）。
3. **1.2 節「舊版極短線衛星策略」實驗**：原本寫死「806次交易、勝率48.88%、淨報酬-0.03%」，這組數字是在更早、更小規模的股票池上跑出來的，股池擴充後已經不能代表現狀。改成只保留核心結論（摩擦成本會侵蝕短線獲利，這個結論本身不受股池大小影響），拿掉無法驗證的精確數字，並在文中明講「刻意不列出」的原因。
4. **3.4 節「回測實驗室」**：原本只有一句話帶過（12MB資料庫、AI網格最佳化、清空紀錄），完全沒提到今天新做的「買入持有回測」——這是目前 backtest.html 排在最前面（第1區塊）的功能，白皮書完全沒提到明顯是重大遺漏。已重寫成完整的 1~5 區塊逐項說明（買入持有回測／同步歷史資料庫／單次回測／AI網格最佳化／大數據回測），並把「12MB」「1,351個交易日」這種寫死數字也改成引導看即時頁面。
5. **5.4 節「AI網格最佳化」說明有真正的事實錯誤，不只是數字過時**：原文寫「500~1000組排列組合」，但實際查證 `backtest_engine.py` 原始碼，AI網格最佳化（Optuna）`n_trials = 20`，只跑20組——「500~1000組」其實是在描述另一個完全不同的功能「大數據回測(Mega Grid)」的窮舉排列組合規模。這是兩個功能被混在一起講述的既有錯誤，不是今天才產生的，藉這次全面校對一併抓出來修正：拆成兩段分別正確說明 AI網格最佳化（Optuna貝氏優化、約20組、秒級完成、適合快速抓方向）跟大數據回測（窮舉所有勾選組合、規模可達數百組、背景執行），並修正後面「如何使用」步驟的標題（原本標題說是在講AI網格最佳化，但步驟內容其實完全是在講大數據回測的操作流程）。
6. **其餘小修正**：`index.html` 掃描狀態文字與機制說明的「70檔」、`backtest.html` 的區塊註解、`analysis.html`／`data_hub.html` 的 `pool_size ?? 70` 前端防呆預設值、`backtest_satellite.py` 的過期註解，全部改成 126（或改成不寫死、動態讀取）。
7. 版本號 `v2.5` → `v3.0`（新增獨立的買入持有回測功能＋股池從70擴充到166，規模足以構成大版號）。
8. `strategy_discussion.html` 裡的「70/30 訓練/測試切分」不是股票檔數，是 Optuna 的訓練/測試集切分比例，跟股池大小無關，確認後不用改。

**已驗證**：doc.html 全文搜尋確認無殘留的「70檔」「806」「1,351」「12MB」等過期字串；瀏覽器開啟確認渲染正常、無 console 錯誤，新增的買入持有回測說明段落與AI網格最佳化修正說明都正確顯示在頁面上。

**目前狀態**：這一大輪（股票清單定案、FinMind bug修正、166檔股池含ETF、下拉選單重做、白皮書全面校對）全部完成並驗證過，尚未 commit（等使用者明確表示這階段的修改都做完了才動手，見 `feedback_commit_timing`）。

## 2026-08-10：Chip Score 是否該納入「乖離率／追高過熱」因子？研究討論全紀錄

**起因**：使用者看到首頁推薦順位 #1 是 6213 聯茂，但這支股票其實是先跌後急拉——07/10 385元跌到 07/30 只剩 239.5元（-37.8%），07/31 起才反轉，7個交易日內從 263 漲到 08/10 收 427.5（+62.5%，中間至少3天漲停）。使用者質疑：模型是不是完全沒考慮「已經漲多、有回落風險」？系統照現有程式碼跑法，究竟第幾天、股價多少就會開始推薦買進？

**第一階段：程式碼追查**（[strategy_core.py:78-117](strategy_core.py:78) `calculate_chip_score`）
- 積分只由兩件事決定：①收盤價是否站上5MA（站上+1，站不上直接-2出局）②當天/前一天法人買賣方向（最多再加+4分）。**完全沒有「連漲幾天」「距低點漲幅」「乖離率」「RSI」這類過熱因子**，排序用的 `momentum`（[main.py:446](main.py:446)，即距20日均線乖離率）只是次要排序鍵，且不扣分只加分，等於變相鼓勵漲越多排越前面。
- 手算6213每日MA5後確認：07/31收盤263 vs MA5=267（還沒站上），**08/03收盤289 vs MA5=263.4 是最早站上5MA的一天**——這是純價格條件下模型最早可能給訊號的時間點，實際還要看當天法人是否同步買超才會真的進榜。
- 過程中額外測試 FinMind 法人資料 API 時，因為當天已被 126 檔全市場掃描用掉大部分額度，我的測試呼叫直接把 IP 暫時封鎖（`retry_after` 約20分鐘）——這不是程式碼問題，是 FinMind 免費額度的既有限制，記錄在案供之後除錯參考。

**第二階段：學術文獻回顧**
- Jegadeesh & Titman (1993) 動能因子研究：過去強勢股未來3~12個月傾向延續，不是均值回歸。
- George & Hwang (2004) 52週高點動能效應：越接近/站上52週高點反而是延續訊號，不是反轉訊號——直接挑戰「離高點越遠越安全」的直覺，AI主題股這種資金集中湧入的多頭階段尤其明顯。
- 業界主流做法：不把乖離率/RSI當「一超過門檻就拒買」的硬性過濾器，而是①跟大盤/族群健康度連動的動態門檻 ②反映在部位大小與停損收緊，而非直接攔截訊號。

**第三階段：拿自家回測資料做實測交叉驗證**（腳本：`/private/tmp/.../scratchpad/momentum_bucket_analysis.py`，純讀取本機 `backtest_database.json` 股價快取 + `backtest_logs.db`，沒打任何外部API，也沒改任何程式碼）
- 取用 experiment_id=2218（4.5年、355筆交易、總報酬333.51%、MDD 58.13%、勝率41.13%，A方案）逐筆重算進場當下的乖離率、距60日低點漲幅、進場前連續漲停天數，分組比對勝率與平均報酬：
  - **乖離率分組**：0~5%組勝率32.8%/平均+0.37%，一路遞增到 >20%組勝率47.2%/平均+3.32%——**越過熱表現越好**，跟52週高點效應預期一致。
  - **距60日低點漲幅分組**：0~10%組最差（勝率20%/平均-1.23%），50~100%組最好（勝率51.2%/平均+4.14%），但**>100%組明顯轉差**（勝率37.9%/平均-0.03%）——有甜蜜區間，但極端值會反轉。
  - **連續漲停天數分組**：0天(314筆)勝率39.8%，1天(26筆)勝率57.7%，2天(5筆)勝率60%，但**3天以上(僅2筆)全數虧損**——樣本數太小，僅供方向參考，不是結論。
  - 用同一份資料對照6213現況：距60日低點漲幅約78.5%（落在表現最好的50~100%區間）、連續漲停天數1天（表現次佳區間）——**它現在的位置巧合地落在歷史表現最好的區間，不是危險區**。

**研究結論**：現行 Chip Score 不納入乖離率/RSI 過熱因子，不是設計疏漏，而是有實證支持——做成硬性排除規則反而會砍掉系統賴以獲利的「強勢延續」交易。真正值得留意的不是「乖離率高」，而是漲幅或連續漲停天數逼近極端值的少數個案，且目前樣本太小不足以下定論。方向性建議（尚未實作）：未來若要處理，應該仿照 [2.3節](doc.html) Risk Parity 精神做「部位縮減/停損收緊」的軟性降權，而不是加一條硬扣分規則。

**已落地的產出**：
1. `doc.html` 新增 **5.6 節「研究筆記：乖離率／動能過熱指標該不該納入 Chip Score？」**，把上述學術觀點＋實測分組結論寫成正式白皮書內容（数字明確標註為「個案研究記錄，非即時數字」，符合白皮書不寫死過時數字的既有原則）。
2. `app.js` 同一輪session的UI修正（跟本研究無直接關聯，一併記錄避免遺漏）：AI推薦卡移除「加入多單追蹤」按鈕與購買成本/操作張數輸入框（改用明日下單建議頁面下單，避免跟操盤室重複輸入）；近7日收盤價新增迷你SVG趨勢線(sparkline)。

**目前狀態**：`calculate_chip_score`／`run_scan` 排序邏輯**完全沒有修改**，這整輪是純研究與白皮書記錄；`app.js` 的 UI 簡化與 sparkline 已測試生效但尚未 commit。若未來要把「極端漲幅軟性降權」真的做進 `strategy_core.py`，建議先擴大驗證樣本（多組實驗、不只2218一組）並修好 [backtest_engine.py:1163-1165](backtest_engine.py:1163) `INSERT INTO trades` 漏寫 `buy_score`/`buy_momentum`/`buy_atr` 的資料缺口，讓未來分析不用每次都回頭重算。

## 2026-08-10（續）：擴大到 A/D 兩方案共8組實驗重複驗證，結論出現重要分歧

使用者要求「擴大到其他幾組表現前段的實驗(A/D策略都跑)重複同樣分析，看結論是否穩定」，判斷 >100%漲幅、3天以上連續漲停 這兩個轉差訊號是不是真的可靠。

**方法**：腳本 `/private/tmp/.../scratchpad/momentum_bucket_analysis_multi.py`，選 A方案4組（2218/2516/2501/2517）、D方案4組（2055/2403/2417/1873）均為總報酬排名前段的實驗，合計 4,247 筆交易、成功配對股價資料 4,217 筆。同樣純讀本機 `backtest_database.json` + `backtest_logs.db`，不打外部API，不改任何程式碼。

**結果與單一實驗（2218）版本的差異**：
- **乖離率分組**：結論不變且更穩固——0~5%組勝率42.6%/平均+1.29%，一路遞增到 >20%組勝率48.7%/平均+3.09%，A、D兩方案分開看都一致遞增。
- **距60日低點漲幅 >100%組：發現這個風險訊號不是兩方案共通的，是 A方案獨有**：
  - A方案4組實驗**全部一致明顯轉差**：2218（勝率37.9% vs 基準41.5%）、2516（30.8% vs 42.8%）、2501（25.0% vs 42.7%）、2517（30.8% vs 43.2%），每組都比自己實驗的基準低約10個百分點以上。
  - D方案4組實驗**幾乎沒事，部分還略優於基準**：2055（46.4% vs 45.3%）、2403（45.9% vs 43.9%）、1873（46.7% vs 44.9%），只有2417略差（40.0% vs 44.0%）。
  - 研判：D方案（2.4節自適應吊燈鎖利，未實現獲利達12%自動收緊停損至1.25x ATR）的出場機制本身已經在吸收極端漲幅回落的風險，A方案沒有對應機制，這是兩方案設計差異的合理結果，不是隨機雜訊。
- **連續漲停3天以上組：樣本從2筆擴大到25筆，信心明顯提升**：勝率24.0%/平均-1.75%/中位數-5.97%，且逐實驗檢查，**8組實驗全部方向一致轉差**（0%~33.3%勝率不等，無一例外優於自己實驗的基準）。雖然單一事件仍偏罕見（每組實驗僅2~5筆），但方向一致性從「單一實驗2筆」大幅提升為「8組實驗全部同向」，可信度已經比初版高很多。

**結論更新**：原本「漲多有風險」的推論要修正為更精確的版本——**過熱風險不是普遍存在，而是 A方案在極端漲幅（>100%）時缺乏防護，D方案的出場機制已經自帶防護**；連續漲停3天以上這個訊號則從「樣本太小不足以下結論」升級為「方向一致性高、值得認真看待，但事件本身罕見」。

**已落地的產出**：`doc.html` 5.6節同步更新，把「A/D方案分歧」與「3天以上訊號信心提升」這兩點寫入白皮書研究筆記，取代原本只用單一實驗（2218）的初版結論。

**目前狀態**：依然是純研究，`strategy_core.py` 完全未修改。若要繼續往下做，下一步應該是：①針對 A方案設計「距低點漲幅>100%時的部位縮減/停損收緊」規則並用同一批8組實驗驗證效果 ②持續觀察連續漲停3天以上這個訊號累積更多樣本後是否維持一致。

**使用者提出的協作偏好（已存成記憶）**：使用者希望之後只要討論中出現有實證支撐、有價值的精簡結論，我應該主動提議放進白皮書，不用每次都明確交代——已存成 feedback 記憶供未來對話使用。

## 2026-08-10（續2）：3:3:4分批 vs 一次買完，全股池對照 + 方法論修正（重要）

**背景**：個案②（6213單股，n=5）不能回答通用性問題，使用者要求擴大到全股池、真正接進回測邏輯驗證。查證發現 `backtest_engine.py` 從來沒有分批進場的邏輯——過去330%等回測數字全部是「一次買滿」跑出來的，3:3:4只存在於 `get_planner_recommendations` 的即時建議文字裡，從未被回測驗證過。

**全股池對照**（現行126檔、資金100萬、持股上限3檔、方案A、2021-01-01~2026-08-07，腳本：`/private/tmp/.../scratchpad/staged_vs_lumpsum_full_backtest.py`，一次買完版直接呼叫正式 `run_backtest`，分批版忠實複製 `get_planner_recommendations` 的30/30/40跨天加碼規則）：
- 一次買完：總報酬379.38%、MDD 44.63%、勝率42.24%、322筆交易
- 3:3:4分批：總報酬133.76%、MDD 30.01%、勝率34.26%、324筆交易，其中**184筆（57%）從頭到尾只完成第一批30%就停損**，只有48筆（15%）真正加滿倉

**第一版空頭驗證（方法有誤，已被推翻，記錄在案作為教訓）**：一開始用「同一條5.5年連續回測裡的交易，依進場當下大盤狀態貼標籤分組」檢驗，得出「空頭震盪期一次買完領先幅度反而更大（+10.7pp vs 多頭+6.7pp）」。使用者質疑這不夠嚴謹（連續回測裡空頭期的部位/資金狀態會延續自之前多頭期，不是乾淨獨立樣本），要求真正獨立跑一個空頭年。

**2022年獨立回測（真正嚴謹版本，結論相反）**：鎖定資料庫最差的一年（2022單獨總報酬-41.11%），獨立跑一次完整回測（不是連續回測裡分段，是真正只用2022-01-01~2022-12-31跑一次）：
- 一次買完：總報酬**-40.85%**、MDD 45.32%、勝率18.52%、獲利因子**0.32**（虧損遠大於獲利）
- 3:3:4分批：總報酬**-21.18%**、MDD 26.78%、勝率17.46%、獲利因子**1.47**（毛利仍大於毛損）
- **分批進場在真正的空頭年明顯優於一次買完**，方向跟第一版完全相反。

**修正後結論**：3:3:4分批沒有「絕對比較好/比較差」，取決於所處市場階段——5.5年混合期間一次買完贏，是因為那段期間多頭年占多數（2023~2026都大賺），一次買完靠複利吃滿多頭紅利；但單獨看真正的空頭年，分批進場的資金保護明顯更好（虧損減半、MDD大降、獲利因子仍為正）。

**方法論教訓（使用者明確要求記錄）**：用「連續回測內按標籤分組」代替「真正獨立跑單一年份」得出的結論不可靠，容易被延續自其他regime的部位/資金狀態污染，兩者不是同一件事。使用者原話：「這就是浪費時間與資源的證明，記得，沒把握或有疑慮可以跟我先討論或敘明」——已存成 [[feedback_research_methodology_confirmation]] 記憶：之後方法論本身有捷徑或潛在confound時，要先講清楚限制或先問過，不要用捷徑算出的結果冒充有把握的結論，等使用者自己抓到錯誤。

**另一個相關教訓（同一輪對話）**：使用者質疑「order_planner沒有分批選項」跟「你卻能回測出3:3:4分批的結果」是否矛盾。釐清後不矛盾：`get_planner_recommendations` 裡的3:3:4規則是真實存在的程式碼定義，只是從未被自動化執行/回測過；我的研究腳本忠實複製這個**已定義**的規則去補回測，不是自己發明新規則。但這也點出一個更廣的原則：**如果研究要驗證的機制在程式碼裡沒有精確定義（例如3:4:3分批出場，`evaluate_exit` 目前完全沒有分批賣出、只有單次全部出場），不能自己編一套設計就開跑，要先跟使用者確認定義或提出市場常見定義讓使用者確認**——已存成同一則 feedback 記憶。3:4:3出場的案例研究目前暫停在「確認範圍」階段，尚未開始模擬。

**已落地的產出**：`case_studies.html` 個案③的「牛市偏誤檢查」段落已修正為2022年獨立回測的正確結論，並在頁面上明確標註修正說明（紅色警示框），不是悄悄改掉。

**目前狀態**：`strategy_core.py`/`backtest_engine.py` 完全未修改，全部是研究腳本產出。首頁標語「3:3:4分批進場」是否要調整文案語氣（目前白皮書3.2節語氣偏向暗示這是最佳做法，但實測顯示是風險-報酬取捨工具，不是報酬最大化工具）——待使用者決定，未執行。3:4:3分批出場案例研究待使用者確認範圍後才開始。

## 2026-08-10（續3）：出場機制（3:4:3）研究 + 加大樣本重跑 + 多空頭regime隔離驗證

**範圍確認**：查證後發現白皮書「3:4:3階梯出場」對應的是「我的操盤室」卡片的純顯示警示邏輯（app.js/history.js的alertHtml），從未接進`backtest_engine.py`，跟真正驅動回測的`evaluate_exit`是兩套不同系統——跟3:3:4進場同樣狀況。四條防線裡「保本停利」在卡片原始定義裡不是賣出動作，是「上調防守線到成本+5%」，這點先跟使用者確認後才動手（見上方[[feedback_research_methodology_confirmation]]教訓的實際應用），測了adjust（照原定義）跟sell（直接出清）兩種處理。

**加大樣本重跑（使用者要求「務求研究扎實精確」）**：持股上限從3檔提高到8檔，同時修正了一次TAIEX抓取的暫時性失敗（yfinance transient issue，非程式碼問題，retry後正常）。3:3:4進場研究也同步用8檔重跑：
- 全期間2021-2026：一次買完314.29%/MDD21.1%/839筆，分批119.17%/MDD20.41%/895筆——**MDD幾乎打平**（3檔時是30.01% vs 44.63%，差距很大）。代表分批的降風險效果，很大一部分其實跟「持股夠分散」重疊，本來就分散8檔的話分批能多降的風險有限。
- 2022空頭年：一次買完-23.94%/MDD30.49%/116筆，分批-5.49%/MDD19.86%/164筆——分批優於一次買完的結論在大樣本下依然穩固。
- `case_studies.html`個案③已更新為這組8檔大樣本數字，取代舊的3檔小樣本版本。

**出場機制8組對照（8檔持股，205~361筆交易，取代原本15~19筆的過小樣本版本）**：
- 卡片四防線(adjust,基準)：140.52%/MDD21.51%/205筆
- 關掉防線A：**316.87%**/MDD23.03%/144筆（拿掉-8%固定停損後報酬、勝率、獲利因子全面提升）
- 保本停利=直接出清：179.46%/MDD21.15%/361筆（優於adjust版）
- 關掉防線B：137.51%/202筆（幾乎打平基準，B沒有明顯貢獻）
- 關掉保本停利：221.29%/MDD19.86%/202筆（連MDD都優於基準，adjust模式本身可能是三個處理方式裡最差的）
- Scale-out分批賣出：126.24%/MDD28.26%/勝率60.61%(全組最高)/66筆
- 「陽春版」（單一固定-8%停損）跟「關掉高點打折鎖利」即使加大持股上限到8檔，交易數仍只有19筆、39筆——這是結構性的（觸發條件太寬鬆，部位長期不出場、抱到360天上限），不是樣本設計問題，這兩組數字不採信。

**組合最佳假設被推翻**：把「關掉防線A」+「保本停利=直接出清」疊加，全期間總報酬216.04%、MDD 36.37%——**兩項指標都輸給只改「關掉防線A」一項**（316.87% / 23.03%）。兩個各自證實有效的調整合在一起會互相干擾，不是1+1效果，事前假設「疊加會更好」是錯的。

**多空頭regime隔離測試（真正獨立單年回測，不是連續回測貼標籤，直接回應「進場空頭年有差異，出場是否也有差異」的提問）**：鎖定2022（最差年，-41.11%）與2024（最強年，+127.18%）分別獨立跑：
- **防線A的角色在多空頭年完全相反**：2022年拿掉防線A表現變差（-19.93%→-20.28%，四組裡最差）；2024年拿掉防線A表現變好（33.65%→34.8%，四組裡最好）。全期間測出的「拿掉防線A比較好」其實只反映多頭年占多數的偏誤——跟個案③3:3:4進場的方法論教訓完全同構。
- 組合方案在2022年反而是四組裡最好（-14.99%），在2024年是四組裡最差（15.17%）——同樣的regime反轉現象。
- Scale-out分批賣出是唯一在三個期間（全期間/2022/2024）都維持前段班、沒有墊底過的方案，勝率分別是60.61%/38.64%/60.87%，穩定度最好。

**研究結論**：沒有「放諸四海皆準」的最佳出場方案，防線A（固定停損）的價值高度依賴市場所處階段，理論上應該像白皮書2.6節巨觀風控分級那樣動態開關，不是固定拿掉或固定保留。若要選一個不用判斷regime的穩健選項，Scale-out分批賣出目前看起來最可靠。

**已落地的產出**：`case_studies.html`新增個案④，完整記錄上述發現；個案③已更新為8檔大樣本數字。

**目前狀態**：`strategy_core.py`完全未修改。進場側的多空頭regime隔離測試目前只做了2022年（空頭），2024年（多頭）尚未做對稱測試——下一步待補。

## 2026-08-10（續4）：進場側2024多頭年補測 + Regime自動切換模型（本輪研究最重要的產出）

**進場側regime對稱補測**：2024年（最強多頭年，+127.18%）獨立跑8檔持股版本：一次買完63.29%/MDD9.09%/146筆，分批11.42%/MDD7.29%/191筆——一次買完明顯勝出，跟2022空頭年（分批勝出）方向完全相反。進場側現在跟出場側一樣，完整驗證了「多頭適合積極、空頭適合保守」的regime依賴性。`case_studies.html`個案③已補上這組2024對照。

**使用者提出的設計問題**：既然多空頭年的最佳參數不同，模型是否該設計成「依當下市況即時判斷、自動切換參數」，而不是固定用一套？這呼應白皮書2.6節三合一巨觀風控（目前只用於「要不要買、買多少」）的設計哲學，使用者希望把這個原則擴大到「怎麼買、怎麼賣」全部參數。

**Regime自動切換模型設計與驗證**（腳本：`regime_adaptive_backtest.py`）：
- regime訊號：當天TAIEX收盤 vs 20日均線，逐日動態判斷，不使用未來資訊。
- 多頭模式：一次買完 + 關掉防線A（2024年實測最佳組合）。空頭模式：3:3:4分批 + 關防線A+保本直接出清組合（2022年實測最佳組合）。
- 全期間2021-2026結果：靜態基準298.72%、靜態最佳單一改動（一次買完+關防線A）394.38%、**Regime自動切換522.18%**——自動切換贏過兩個靜態方案，總報酬比目前最好的靜態方案高約33%，勝率也最高（53.11%），MDD維持在相近水準（22.25%），不是用犧牲風控換來的。

**方法論限制與樣本外檢查（主動補做，非使用者要求）**：兩條子規則本身取自2022、2024兩個獨立年份的贏家設定，而驗證切換效果的全期間測試又把這兩年算在裡面，不是完全乾淨的樣本外驗證。額外拉出各年度單獨損益檢查：2021/2023/2025/2026（沒被用來挑規則）全部正報酬、勝率50~63%；2022（唯一用來挑空頭規則的年份）反而是六年裡勝率最低的（40.5%）。這代表整體表現不是靠兩個「主場年」撐起來的，大幅降低過度配適疑慮，但樣本仍只有5.5年、每個regime僅有限個完整年份，稱不上長期穩健驗證。

**研究結論**：以即時可算、不使用未來資訊的regime訊號動態切換進出場參數，確實贏過目前測過的任何單一固定方案，驗證了「依市況判斷適合參數」這個設計方向本身有效，不只是巧合。這是本輪研究（乖離率→3:3:4進場→出場機制→regime自動切換）四個獨立子研究收斂出的統一結論。

**已落地的產出**：`case_studies.html`新增個案⑤，完整記錄切換規則設計、全期間結果、樣本外檢查與方法論限制。

**目前狀態**：`strategy_core.py`與所有正式頁面（含首頁）完全未修改，這是使用者明確要求的順序——先在背景把研究做扎實，得出好結果後才考慮做成介面功能（例如使用者設想的「首頁系統即時預判推薦排序」）。若要正式導入，建議：①累積更多獨立空頭年樣本再上線 ②先以研究/選配功能形式提供，不直接覆蓋現有固定方案的預設行為。

## 2026-08-10（續5）：A/B/C/D/E五方案全面對照 + regime模型定位修正 + 首頁文字調整 + git checkpoint

**重要修正：regime模型不是「限定多頭使用」，是使用者對樣本年限的解釋被我誤解**。使用者澄清：接受5.5年樣本量足夠，原因是**股票池全部是AI概念股，AI題材本身只有約5年爆發期可回測，往前拉長沒有對應真實資料**，不是刻意把模型範圍縮小成只在多頭用。使用者明確要求：**regime模型的目標是無論多頭或空頭，系統都要能依當下狀況自動判斷、給出最佳進出場建議**——已修正 [[project_regime_adaptive_promotion_reminder]] 記憶裡的錯誤框架。

**A/B/C/D/E五方案全面對照**（一次買完+各方案原生`evaluate_exit`邏輯，8檔持股，直接呼叫正式`run_backtest`，共15次正式回測）：
- 全期間2021-2026：A最強（314.29%，MDD 21.1%全部最低）> D（246.98%，但MDD 41.59%）> C（206.64%）> B（191.93%）> E（188.41%）
- 2024多頭年：A最強（63.29%，MDD 9.09%最低）> C（34.7%）> B（30.82%）> E（24.43%）> D（21.57%）
- **2022空頭年：B最強（-20.64%虧損最少，MDD 23.55%最低）** > E（-24.1%）> A（-23.94%）> C（-27.11%）> **D最差（-35.65%，MDD 41.53%）**——D的全期間亮眼表現其實是承擔最高空頭風險換來的。

**結論**：方案A確認是多頭/全期間的最佳基底（[[project_strategy_analysis_notes]]個案⑤選A當多頭端沒有問題），但**空頭端最強的是方案B，不是A**——這正是使用者要的「依市況自動選最佳方案」的具體例子。個案⑤原本的regime模型空頭端沿用的是方案A的邏輯（A在空頭年只是中段班），不是真正的空頭最佳解。

**Regime v2重新設計並驗證中**（腳本：`regime_adaptive_v2_A_bull_B_bear.py`）：多頭端維持「一次買完+方案A原生出場」（已驗證最強），空頭端改成「3:3:4分批+方案B原生出場」（取代原本的card-line組合，改用剛驗證出的空頭最強方案）。跑完會補上結果對照。

**已落地的產出**：
1. `case_studies.html`新增個案⑥（五方案全面對照），並在個案⑤補充「樣本年限受限於AI題材本身歷史」的說明，避免誤解成刻意限縮範圍。
2. 首頁四處文字調整（使用者明確指示）：①首頁標語拿掉「3:3:4分批進場｜3:4:3分批出場｜」，只留「多空雙向即時追蹤機制(雙端智慧連線版)」②署名從「宇君 2026.8.9 更新」改成「juj 2026.8.10 更新中」③`order_planner.html`標題加註「（需先於首頁中同步真實盤後數據）」④`history.html`「戰情損益統計看板」加註「（需於明日下單建議中下單）」。
3. **git commit checkpoint**：`0c174fb`，使用者明確要求「確認範圍要現在做一次git commit當還原點」後才commit，涵蓋這次session截至該時間點的所有修改（`case_studies.html`新建、`research_data/`資料夾、`main.py`/`app.js`/`doc.html`/`backtest.html`/研究筆記）。尚未push到GitHub，維持本地。

**目前狀態**：Regime v2（A多頭+B空頭）背景跑中，結果待補。首頁文案調整已直接套用（使用者這次是明確指示，不是研究階段的謹慎待確認事項）。`strategy_core.py`核心邏輯仍未修改。

## 2026-08-10（續6）：Regime v2（方案B重新設計空頭端）驗證失敗 + 重現性方法論修正 + 又一批文字調整

**方法論問題發現與修正**：Regime v2第一次測試時，同一支腳本、同樣參數跑兩次得到不同結果（200.54% vs 162.67%）。追查後發現腳本每次執行都即時呼叫yfinance／FinMind抓取TAIEX與巨觀風控資料，FinMind常態性逾時／不穩定，導致同樣程式碼因外部API當下狀態不同而得到不同輸入資料，結果不可重現。修正做法：先把TAIEX、巨觀風控資料各抓一次存成本地固定快取（`cached_taiex_macro.json`），之後所有重跑一律讀取同一份快取。重跑兩次確認結果完全一致（251.51% / 162.67%，分毫不差）後才採信。**這是一個通用教訓：任何依賴即時外部API（yfinance/FinMind）的研究腳本，都該比照這個做法先固定快取輸入資料，否則同一支腳本兩次執行可能得到不同「結論」。**

**Regime v2結果（可重現版本，腳本：`regime_adaptive_v2_A_bull_B_bear.py`）**：多頭端沿用方案A原生出場（已驗證最佳），空頭端改用方案B原生出場（個案⑥驗證出的空頭最強方案）+ 3:3:4分批進場，取代個案⑤的card-line組合：
- 靜態方案A（全程不切換，此次因巨觀風控fail-open）：251.51% / MDD 27.41% / 965筆
- **Regime v2（A多頭+B空頭原生）：162.67%（更差）/ MDD 37.18%（更差）/ 891筆**
- 對照個案⑤ Regime v1（空頭用card-line組合，同樣fail-open狀態）：522.18% / MDD 22.25% / 273筆

**結論：用方案B原生邏輯取代空頭端是一次驗證失敗的嘗試，比原本card-line組合版本差非常多，甚至比全程只用方案A還差。** 這跟個案④「組合最佳假設不成立」是同一種現象——方案B個別測試是空頭最強，但整合進regime系統後不保證整體最強。目前個案⑤的card-line組合版本仍是測過最好的regime設計，未被超越。

**使用者再次強調的核心訴求（已修正先前記憶中的誤解）**：使用者明確表示regime模型的目標是「無論多頭或空頭，系統都要能依當下狀況自動判斷、給出最佳進出場建議」，不是限定多頭使用；5.5年樣本量的接受，純粹是因為AI概念股題材本身只有這段爆發期可回測，不是刻意縮小模型範圍。[[project_regime_adaptive_promotion_reminder]]記憶已同步修正。

**首頁與其他頁面文字再調整（使用者明確指示，直接套用）**：`backtest.html` 的環境隔離提示文字，從「在此環境進行的任何參數調整與模擬交易，皆與生產環境完全隔離。請依序完成以下步驟。」改成「選擇不同的條件去做回測，記得要先同步歷史資料庫。（ 管理者方可使用，但其他人可看到測試數據以做分析研究 ）」。

**已落地的產出**：`case_studies.html`新增個案⑦，如實記錄這次失敗的嘗試與重現性修正過程（不因結果不如預期就隱藏）；個案⑥結論段落同步更新，指向個案⑦的實測結果。

**目前狀態**：`strategy_core.py`完全未修改。下一輪若要繼續優化空頭端，個案⑦建議的方向是「只借用方案B的進場篩選條件，出場仍用card-line組合」而非整套換成方案B原生邏輯——尚未測試。

## 2026-08-10（續7）：修正方向後成功——土洋雙賣疊加版，本日最後一組結果

**修正**：查證 `evaluate_entry` 發現方案A/B/C/E的進場篩選邏輯完全相同（只有方案D有額外濾網），「借用方案B的進場篩選」其實是無效提案（跟A沒有差異）。方案B真正的差異只在出場端一條規則——「土洋雙賣」（外資與投信同一天都賣超就立即全部出清）。改成把這條規則當作**新增的第五條防線疊加**進個案⑤原本的空頭card-line組合（不是整套換掉），先修好腳本的可重現性問題（TAIEX改吃本地固定快取 `cached_taiex_macro.json`，不再每次即時fetch），重跑後個案⑤原版分毫不差重現522.18%，確認整支腳本可信。

**疊加土洋雙賣結果**（腳本：`regime_adaptive_v3_add_dualsell.py`）：
- 個案⑤原版：522.18% / MDD 22.25% / 勝率53.11% / 273筆
- **疊加土洋雙賣：468.71% / MDD 17.48%（明顯更低）/ 勝率44.09% / 465筆**

**結論**：不是單純的贏或輸，是報酬換防守的取捨——總報酬降約53個百分點，MDD改善近5個百分點，交易周轉大增。方案B的空頭優勢這次找到有效整合方式（疊加而非替換），但仍是取捨關係，未決定要不要採用疊加版當標準版本。

**已落地的產出**：`case_studies.html`個案⑦補上這段完整記錄（含失敗的整套替換、修正後的疊加成功版本、可重現性修正）。

**今日研究總結（供明天接續參考）**：本日從「6213聯茂排名疑問」出發，一路做到：①乖離率不該當硬扣分（個案①）②3:3:4分批vs一次買完的多空頭regime依賴性（個案③）③出場防線的多空頭regime依賴性（個案④）④regime自動切換首版驗證有效（個案⑤，522.18%）⑤A/B/C/D/E五方案全面對照，A確認多頭最佳、B確認空頭最佳（個案⑥）⑥嘗試把B整合進空頭端，整套替換失敗、疊加土洋雙賣則是有效的風險-報酬取捨（個案⑦）。全程 `strategy_core.py`/`backtest_engine.py` 未被修改，全部是研究腳本產出；`case_studies.html` 目前有7個案例，完整記錄含失敗嘗試。首頁與多個頁面的文字也依使用者指示調整過（標語簡化、署名更新、頁面說明文字補充）。git commit checkpoint 為 `0c174fb`，之後的變更（case_studies.html個案⑤⑥⑦更新、5處文字修改、research_data無關的內容）尚未再次commit。

## 2026-08-11：GitHub合併、7個案例的原始資料檔案連結、巨觀風控快取正式修復、C/D/E網格、空頭端窮舉搜尋

**GitHub同步**：本地與遠端各自超前（本地2筆研究相關commit、遠端7筆，含資安修正「移除local-request bypass，強制要求管理者驗證」等）。兩邊改動檔案完全不重疊，`git pull --no-rebase` 乾淨合併無衝突（merge commit `b61802d`）。合併後重啟兩個本地server確認正常。

**交易成本說明已補進白皮書研究頁**：`case_studies.html` meta-box新增說明——所有個案報酬率都用回測實驗室「單邊交易成本0.3%（來回0.6%）—真實嚴苛」這組預設值，依白皮書明確定義，這是手續費+證交稅+高價股滑價懲罰打包的合併費率，不是只扣手續費漏扣稅。

**七個個案的原始回測資料檔案，全部連結進 `case_studies.html`**：新增 `research_data/case1~case7`（含case7兩個版本）共8個CSV檔案，`main.py` 的 `/research_data/{filename}` 白名單同步擴充，`README.md` 補齊每個檔案的說明。過程中發現個案⑤的資料檔曾被個案⑦的後續實驗覆蓋掉（檔名衝突），已重新單獨跑一次乾淨版本（`case5_regime_v1_trades.csv`，273筆，522.18%分毫不差）確保連結的是正確資料。

**巨觀風控資料快取——真正的正式程式碼修復（不是研究腳本）**：查證 `fetch_macro_3in1_series` 發現這三個巨觀指標（外資期貨淨空單／台幣匯率／外資現貨買賣超）從來沒被「同步歷史資料庫」流程存進本地 `backtest_database.json`，跟股價/籌碼是分開的兩條路，每次回測都要即時打3支FinMind API，逾時或配額用完就整批fail-open（風控悄悄沒套用，沒有任何錯誤提示）。修復方式：
1. `fetch_macro_3in1_series` 改成優先讀本地 `db["macro"]` 快取（只快取原始數值，veto/pos_scale即時算，避免風控邏輯以後調整時舊快取失效），只有快取沒涵蓋查詢範圍才退回即時抓取；即時抓到的也會寫回本地快取。
2. `/api/backtest/download`（「同步歷史資料庫」按鈕）新增步驟3，比照股價/籌碼的斷點續傳邏輯主動同步這三個指標。
3. 實測驗證：空快取時抓 2021-01-01~2026-08-11 全範圍花9.5秒、1,377筆全部存入本地；重複呼叫同一範圍改吃快取，0.225秒完成（原本4.4秒），確認正常運作。已重啟 `main.py`/`backtest_engine.py` 兩個server套用。

**這個修復對今天所有regime研究的意義**：之前個案⑤⑦的regime自動切換全部是在巨觀風控fail-open狀態下跑出來的（522.18%、468.71%等數字都沒套用風控），現在快取修好之後，理論上可以重跑一次「巨觀風控真正生效」版本的regime模型，數字可能會不一樣——這是下一步可以做的驗證，今天還沒重跑。

**方案C/D/E全部納入regime組合網格（5x5=25組，使用各方案原生`evaluate_exit`，非card-line微調版）**：
- 沒有任何純方案配對打贏個案⑤的card-line微調版（522.18%）——最高的是E多頭+C空頭（244.83%），其次D多頭+A空頭（234.29%，MDD 28.72%，五方案網格裡風控最好、獲利因子1.57最高）。
- **結論**：真正拉開差距的不是「選哪個內建方案」，是個案④那組針對性微調過的card-line規則細節（拿掉防線A、保本停利改直接出清），沒有一個方案的原生出場邏輯打得贏微調版。D當多頭基底這個新組合（D+A）的風控表現值得記錄，但仍非最優解。
- 完整25組結果：`regime_grid_all_strategies_results.json`。

**空頭端出場規則窮舉搜尋（48組，5個開關全組合：防線A/防線B/高點打折鎖利 各on/off、保本停利off/adjust/sell、土洋雙賣on/off，多頭端固定用個案⑤已驗證最佳設定）**：跑完，找到明顯打贏先前手動挑選版本的新冠軍。

前5名（依總報酬排序）：
1. **692.64% / MDD 24.17% / PF 3.68** — 防線A關、防線B關、高點打折鎖利關、保本停利adjust、+土洋雙賣
2. 682.76% / MDD 24.24% / PF 3.56 — 防線A/B/高點鎖利/保本停利全關，**只留土洋雙賣單一條件**
3. 669.84% / **MDD 19.78%（全組最低）** / PF 3.78 — 防線A關、防線B開、高點鎖利關、保本停利關、+土洋雙賣
4. 656.52% / MDD 22.92% / PF 3.72 — 防線A關、防線B開、高點鎖利關、保本停利adjust、+土洋雙賣
5. 596.51% / MDD 22.09% / PF 3.33 — 防線A關、防線B開、高點鎖利開、保本停利關、+土洋雙賣

對照：個案⑤原版（522.18%）排名第30。

**規律**：前5名全部一致——**防線A一定關掉**（跟個案④/⑥的發現同構），**土洋雙賣一定加上**，48組裡沒有例外。高點打折鎖利在前5名裡4組也是關掉的，顯示這條防線可能也在幫倒忙。第2名尤其極端：空頭端幾乎不設任何防線，只用土洋雙賣單一條件，總報酬682.76%仍是全部48組第二高，代表「規則做多不代表做得好」，土洋雙賣這個單一條件本身就有很強的解釋力。

**目前最佳版本**：防線A/B/高點打折鎖利全關，保本停利用adjust模式，加上土洋雙賣——692.64%，比個案⑤原版高約33%，比先前的疊加版（468.71%）高約47%。這是系統化搜尋而非手動判斷找到的，符合使用者要求「窮盡完成」。

**已落地的產出**：`case_studies.html`個案⑦新增「窮舉搜尋」小節，完整記錄前5名+對照表+規律歸納；新增`research_data/case7_bear_exhaustive_48combos.csv`（48列）並連結進頁面；`main.py`白名單同步更新。

**交叉驗證：巨觀風控真正生效後重跑48組——冠軍配置完全不變**。使用者明確要求「先完成用風控真正生效的狀態把48組重跑一次」。修改 `regime_bear_exhaustive_search.py` 加入 `fetch_macro_3in1_series`（這次真的讀到本地快取，不再fail-open）與veto/pos_scale邏輯後全部重跑：
- fail-open版冠軍：692.64% / MDD 24.17%（防線A/B/高點鎖利全關，保本adjust，+土洋雙賣）
- **風控生效版冠軍：488.17% / MDD 19.98%（更低）——配置完全相同**
- 絕對報酬下降、MDD進一步改善，都是風控生效的預期效果（風險週擋單/減碼會壓低複利，但也確實降低回撤），**不是結論被推翻，是同一個結論被兩種風控狀態獨立驗證**。
- 但第2~5名排序有變動，風控生效版第5名（428.78%）防線A是開啟的，代表「前5名一律關防線A」這條規律沒有先前想的絕對，只有冠軍本身（#1）跨兩種狀態都穩定。兩版MDD最低前5名裡毫無例外都有`use_dualsell=True`——這是目前信心最高的單一發現，比「防線A必關」更可靠。

**已落地的產出**：`case_studies.html`個案⑦新增「交叉驗證」小節（含對照表、規律修正說明）；新增`research_data/case7_bear_exhaustive_48combos.csv`（fail-open版）+ `case7_bear_exhaustive_48combos_macroON.csv`（風控生效版）並連結進頁面；`main.py`白名單同步更新兩個檔名。

**目前狀態**：`backtest_engine.py` 的巨觀風控快取修復是本次session唯一真正動到正式程式碼（非研究腳本）的地方，已測試驗證且已用於重跑48組交叉驗證，但這個生產程式碼修復本身尚未commit。`strategy_core.py` 完全未修改。regime模型空頭端目前最佳、且經風控開關交叉驗證仍成立的設定是「防線A/B/高點鎖利全關+保本adjust+土洋雙賣」，仍是研究階段，未套用到即時系統。

**新增完整研究報告 `research_report.html`**：使用者要求「將測試與回測的理論、步驟和程序都說明清楚，做成接近學術水準的報告，數據處連結資料檔案，連同參考資料置於個案研究網頁中」。新建獨立頁面（非併入case_studies.html，避免原本的逐案陳述格式被打斷），結構：摘要／1.研究動機／2.資料與方法（統一參數、兩種回測方法說明）／3.研究發現（依邏輯順序把個案①～⑦整合成連貫敘事，非條列七個獨立區塊）／4.方法論教訓（4個獨立小節：不可重現陷阱、連續回測分段謬誤、機制需先確認定義、巨觀風控從未快取）／5.局限性／6.結論與後續方向／7.附錄資料檔案總覽表。全文提到數據的地方都用 `<a class="data-link">` 連結到 `research_data/` 對應CSV。`case_studies.html` 最上方新增顯眼連結導向此報告。`main.py`白名單同步加入`research_report.html`。已重啟server驗證：報告頁200、案例研究頁200、內文連結的資料檔案抽查200。

## 2026-08-11（續）研究成果第一階段導入首頁 + 大數據報告/個案研究補強

**先做checkpoint**：使用者要求「標記今天修改之前的版本，以免要回頭」，在動任何首頁程式碼前先commit了research_report.html/research_data/case_studies.html/backtest_engine.py/main.py的既有變更（commit `2600b31`），確保有明確回滾點。

**首頁 Stage 1（研究版advisory面板，依先前討論的三階段導入方案，使用者已核准「逐步開始做」）**：
- `main.py` 新增 `GET /api/regime/status`：即時抓TAIEX（重用`backtest_engine.fetch_taiex_history`）算「當日收盤 vs 20日均線」，訊號定義跟研究腳本`is_bull()`完全一致；依bull/bear回傳個案⑤⑦驗證出的建議進出場方式文字。比照`daily_scan_cache.json`模式做每日一次本地快取（`regime_status_cache.json`），避免每次使用者載入首頁都即時打yfinance（見yfinance rate-limit記憶規則）；快取當日失效時原地重抓、抓取失敗則退回顯示上次快取結果並附註。
- `index.html`/`app.js` 新增`regimePanel`區塊，首頁載入時呼叫上述API並渲染：目前regime（多頭/空頭）、TAIEX收盤/MA20/乖離%、建議進場方式、建議出場防線組合、以及連到`research_report.html`與`case_studies.html#case5`的連結，明確標註「研究版建議⋯系統實際下單建議目前仍採單一固定模型」。**不影響**現有AI推薦排名、下單建議、任何既有功能——純附加、可隨時關掉。
- 瀏覽器實測：登入後首頁正確顯示「多頭」（TAIEX 45120.72 vs MA20 43615.12，乖離+3.45%），無console錯誤，網路請求200。

**大數據回測量化分析報告（`analysis.html`）新增第六節**：使用者指出這份報告需要根據regime研究更新。原報告第一~五節的排行榜/統計全部建立在「整個5.5年套同一組固定參數」的前提上，沒有反映regime研究的發現。新增「六、延伸研究：Regime自動切換能否贏過任何單一固定方案？」，說明這個前提本身的侷限、列出48組窮舉冠軍的兩種風控狀態下數字（692.64%/MDD24.17%、488.17%/MDD19.98%），並連回research_report.html與case_studies.html，同時清楚註明此邏輯尚未寫入正式系統。瀏覽器實測確認渲染正常。

**`case_studies.html` 新增個案⑧「Regime自動切換策略模型的建構與驗證」（總結案）**：使用者明確指名要把這個主題放進個案研究頁。個案⑤⑥⑦原本是三段各自獨立的實驗記錄，新增個案⑧把「從訊號定義→子規則來源→切換驗證→基底方案驗證→失敗嘗試+方法論修正→疊加→窮舉→交叉驗證」8個步驟串成單一時間順序敘事，附最終驗證配置表、績效數字、誠實列出的驗證程度與局限性、目前狀態（尚未寫入strategy_core.py，唯一正式落地是首頁研究版面板）。不重複個案⑤⑥⑦的詳細數據，只做導覽層級的總結+連結。case-nav同步新增連結。瀏覽器實測確認渲染正常、無console錯誤。

**目前狀態**：`main.py`（新增regime endpoint）、`index.html`/`app.js`（首頁面板）、`analysis.html`（第六節）、`case_studies.html`（個案⑧）尚未commit。`strategy_core.py`仍完全未修改——regime邏輯目前只存在於研究報告、個案研究頁、以及首頁的advisory-only面板，尚未成為系統實際下單建議的一部分。

**首頁 Stage 2（面板可收合，範圍最小版）**：使用者要求開始做第二步驟，先用 AskUserQuestion 確認範圍——選了「只切換研究版面板本身的呈現方式」，不動AI推薦卡片任何顯示或排序（比另外兩個選項風險更低）。`app.js` 把 `loadRegimeStatus` 拆成「抓資料」與「渲染」兩段：`regimeStatusData` 存最新一次抓到的regime狀態，`renderRegimePanel()` 依 `localStorage['regimePanelHidden']` 決定顯示完整面板（含右上角「✕ 隱藏」按鈕）或收合成一行小連結（「🧭 研究版市況面板（目前：X，已隱藏）點此顯示 →」，仍會顯示當下regime方便使用者判斷要不要展開）。`index.html` 新增 `regimePanelCollapsed` 這個佔位div。瀏覽器實測：隱藏後reload頁面確認偏好有存住、點連結展開、點按鈕收合，兩個方向都用真實UI點擊測試過，AI推薦卡片區塊不受影響，無console錯誤（唯一出現的403跟regime面板無關，是既有的其他資源請求）。

## 2026-08-11（續）掃描池同步bug修復 + 排序/高價股偏多調查 + Stage 3（R策略正式接入）

**修復：首頁掃描永遠卡在70/126**：使用者回報「強制重新掃描」不管按幾次都補不到126檔，且掃描公告文字出現空括號。追查發現 `app.js` 有一份寫死的 `AI_STOCKS_CONFIG`（含股名，首頁顯示用），只更新到「2026-08-05矽光子供應鏈補強」那批（70檔），但後端 `ai_stock_list.txt` 早在「2026-08-09擴充AI硬體供應鏈六大類」時就有126檔——首頁掃描永遠只送出70檔給後端，`is_full_pool_scan`（要求≥80%股池覆蓋率）永遠判定失敗，掃描結果既不存入當日快取、回應也不含 `cache_date`（=空括號的根因）。修復：從 `stock_metadata.json` 撈出缺的56檔正確中文名稱與參考價，補進 `AI_STOCKS_CONFIG`；`app.js` 五處banner文字改成 `cache_date` 為空時不顯示括號。瀏覽器實測：強制重新掃描兩次後達到126/126。

**調查：推薦排序高價股偏多**：使用者觀察「幾乎無低價股」。查證：`chip_score`只看法人買超正負號、`momentum`是`(close-MA20)/MA20`的%，兩者理論上都跟股價無關（scale-invariant），公式本身沒有偏袒高價股的設計。但實測今天與前兩天（皆126檔全量）top15中位數股價都在800~950元，是整體池中位數（~310元）的2.5~3倍；百元以下股票平均chip_score只有0.32，千元以上高達1.52。結論：這是目前AI股行情本身把籌碼與動能集中在已經很貴的龍頭股，不是模型bug。同時發現排序tie-break（chip_score/momentum完全同分時）依賴多執行緒抓取完成順序，非確定性——已告知使用者，尚未動手修正（次要，未列入本次任務）。

**調查：只買1~2檔的回測有沒有做過**：使用者以為沒測過，查SQLite `backtest_logs.db` 發現 `max_positions=1` 有56組、`=2` 有112組正式實驗（非grid trial）。結果：1檔最佳僅136%總報酬但MDD高達69.67%，2檔最佳306.83%/MDD51.14%，都遠差於8檔的314~320%/MDD僅21%——集中持股風險大增、報酬沒有比較好，資料庫既有結果已可回答，不需要另外測。

**調查：價格區間限定候選池回測**：查證 `backtest_engine.py`/`main.py` 全部程式碼，確認首頁的股價區間下拉選單純粹是前端顯示過濾，回測引擎完全沒有「依價格區間限制候選股池」的機制，這個確切問題現有資料庫回答不了。用51萬筆歷史交易做了一個回溯性（非真正限定候選池）分桶分析：買進價>1000元的交易平均損益1.95%／勝率47.9%，明顯優於<100元的0.58%／41.9%——但這只是相關性，不是因果測試。使用者未要求現在動手做真正的限定回測。

---

**Stage 3：Regime自動切換（R）正式接入 `strategy_core.py`（opt-in，不影響任何現有使用者）**

使用者確認「回到首頁三階段規劃，做第三階段」。先用 AskUserQuestion 確認兩個關鍵設計決定：① 上線方式——新增選項（推薦），不取代A/B/C/D預設，現有持倉/使用者完全不受影響；② 多頭建倉方式——第一次就建議買滿100%（符合個案⑤⑦⑧驗證的「多頭=一次買完+關防線A」）。

過程中發現兩個原先低估的落差，都先跟使用者確認才繼續寫：
1. 「明日下單建議」其實**已經有一套永遠在跑的30/30/40分批機制**（跟研究的3:3:4分批同概念），研究假設的「多頭一次買完」在現有系統裡是全新分支，不是切換既有選項。
2. card-line規則（防線A/B/高點打折鎖利/保本停利/土洋雙賣）**只存在於研究腳本，`strategy_core.py`從來沒有這套邏輯**，是第一次搬進正式程式碼。

**落地內容**：
- `strategy_core.py`：`evaluate_exit()` 新增 `is_bull_regime` 參數（預設None，不影響A/B/C/D/E既有呼叫端）與 `strategy='R'` 分支，呼叫新的 `_evaluate_regime_exit()`——完全繞開A-E共用的吊燈式ATR停損/固定15%停利（兩套機制不共用不疊加）。R的規則：多頭關防線A（防線B/高點打折鎖利/保本停利adjust維持）；空頭防線A/B/高點打折鎖利全關，只留保本停利adjust+土洋雙賣——跟個案⑧驗證的最終配置逐項對應。
- `main.py`：新增 `get_taiex_regime_df()`（沿用`backtest_engine.fetch_taiex_history`，不重複實作抓取）。`_simulate_holding_exit_signal`（`/api/portfolio/sell_check` 用）支援`exit_strategy='R'`：計算進場當下20日低點當`supp`（防線B用）、逐日重新查regime（不是凍結在進場當下，跟研究腳本行為一致）。`get_planner_recommendations` 新增 `use_regime` 參數：多頭時新標的與未買滿的既有持倉都直接建議補到100%（跳過分批），空頭維持既有30/30/40；`potential_buys`/`buys`/`filtered_buys`都新增`exit_strategy`欄位往下傳。`commit_planner_orders`：`CommitOrder`新增`exit_strategy`欄位（預設"D"，不填=完全跟今天以前一樣），R的新倉會即時抓一次真實股價算真正的20日低點當`supp`（不是其他方案用的成本價打95折估算值）。
- `order_planner.html`/`order_planner.js`：新增「🧭 套用Regime自動切換（研究版）」勾選框，預設不勾（完全不影響現有行為），勾選後串`use_regime`參數、顯示套用結果的說明列、buy卡片的exit_strategy會跟著送回commit端點。
- `backtest_engine.py`：`run_backtest`接上`is_bull_regime`（沿用run_backtest原本就有載入的`taiex_df`）與進場時的20日低點`supp`，讓「AI獨立回測實驗室」也能選R持續驗證。**已知落差**：這個engine對所有方案都沒有分批進場機制，R在這裡測出的數字用的是單筆一次到位，會比研究報告的692.64%/488.17%保守，兩者不能直接對比——已在程式碼註解與`backtest.html`下拉選單說明文字中清楚註明。
- `backtest.html`：出場方案下拉選單新增「方案R」選項並附上這個落差的說明文字。

**測試中抓到一個真bug**：`_evaluate_regime_exit`裡`if use_dualsell and today_chip:`對pandas Series做真假值判斷會直接丟`ValueError`（ambiguous truth value）——`today_chip`來源可能是dict、Series或`{}`，改成只用`.get()`取值、不判斷`today_chip`本身真假，修正後兩種呼叫路徑（main.py即時回放、backtest_engine逐日回測）都正常。

**驗證**：① `/api/planner/recommendations?use_regime=true`（唯讀）確認多頭時5檔全部標`首批 100%（Regime多頭）`+`exit_strategy=R`，`use_regime=false`/未帶參數確認完全跟過去一樣（30%分批+方案D）；② 用合成假倉位直接呼叫`_simulate_holding_exit_signal`（沒有動到真實`portfolio.json`）驗證R的賣出判斷會正常跑完不出錯；③ 用`/api/backtest/run`實際跑R策略，2024-2025（多空交替）144.95%/MDD23.99%，2022純空頭年-13.51%/MDD25.66%、出場原因39/46筆是土洋雙賣（跟個案⑦「土洋雙賣是信心最高的單一發現」一致），中間也出現少數高點打折鎖利/防線B出場（代表2022年內也有短暫多頭子期間，regime確實逐日動態切換，不是凍結在年初判斷）；④ 瀏覽器實測order_planner.html勾選框，畫面正確顯示「目前判定多頭」與規則說明，卡片正確顯示「首批100%（Regime多頭）」，network無錯誤。全程沒有對真實`portfolio.json`做任何測試性寫入。

**目前狀態**：`strategy_core.py`、`main.py`、`order_planner.html`、`order_planner.js`、`backtest.html`、`backtest_engine.py` 均已修改但尚未commit。R是完全opt-in設計，沒勾選/沒指定的既有使用者與既有持倉行為分毫不變。

**使用者要求把R改成預設開啟**：「兩種方法顛倒」——`order_planner.html`的勾選框改成預設打勾（`checked`屬性），取消勾選才會退回傳統方案D+30/30/40分批。同步更新提示文字（打勾時顯示regime套用結果，取消時顯示「已改用傳統方案」提示）。已提醒過一次研究驗證仍有侷限（空頭端只有2022一年完整樣本），使用者知情後仍要求這樣做，尊重其決定，之後不再每次重複提醒。

**修正：`portfolio.json`裡6213聯茂缺`buy_date`欄位**：導致`/api/portfolio/sell_check`回報「缺少buy_date或cost，無法回放模型判斷」。查證這筆持倉本身沒有進場日期，依`cost`(427.5)與`history_prices`裡`08-10`當天的收盤價完全吻合，推斷買進日應為2026-08-10，補上後恢復正常。

## 2026-08-12 首頁掃描/籌碼快取架構問題 + yfinance資料延遲bug + 連續同買天數研究（含推翻前一版結論）

**修正：`get_planner_recommendations`（明日下單建議）市況警語每次整理都不一樣**：使用者回報「今天明明是一樣的最新資料，警語卻不一樣」。查證：這支API在當日掃描快取還沒建好時，會自己觸發即時掃描，但直接用`cache_db[today_str] = scan_results`整批覆蓋，沒有像`/api/scan_all`那樣做「合併不覆蓋」。今天FinMind/yfinance不穩，每次即時掃描抓到的檔位都不一樣，導致健康度/市況跟著每次不同。修正為比照`/api/scan_all`的合併邏輯。

**本機伺服器卡死排查**：使用者回報「網頁當了不能用」。查證：伺服器process還活著但完全無回應（curl逾時），追查是新的一天（跨日）+ FinMind額度問題疊加，某段程式碼卡在重試/逾時迴圈。重啟伺服器解決；後續持續在這次session觀察到FinMind回應變慢/失敗。

**深入查證：「籌碼資料獲取受限」(is_mock)是真的，且找到真正根因**：直接呼叫FinMind API確認回傳 **HTTP 402 "Requests reach the upper limit"**——是**每日API額度用盡**，不是網路逾時。合理推測是這次session大量研究/測試（回測、即時掃描、多次重啟)把免費額度用光。修正：①`fetch_chip_data_from_finmind`加一次重試（對真正暫時性逾時有幫助，對402額度問題沒用但無害）；②非success回應時印出實際原因（分辨額度問題vs真連線問題）；③最重要的架構修正——`chip_cache`原本只存記憶體、只有1小時TTL，使用者明確指出「首頁抓過的資料，操盤室應該直接沿用，不用再抓」，查證後這是合理且更嚴重的問題：**任何一次伺服器重啟（這次session因為改程式碼重啟了七八次）都會把快取整個清空**，逼所有股票重新抓一輪，很可能是這次額度被用光的主因之一。改成存成`chip_cache.json`存磁碟、以「交易日」為單位快取（不是1小時輪替），重啟伺服器也不會清空，首頁跟操盤室共用同一份。實測驗證：模擬今天已成功快取的資料，第二次呼叫完全不再打FinMind（log 0筆FinMind呼叫），確認共用生效。FinMind失敗時也改成優先用上次真實資料當備援，不再顯示全部歸零的假資料。

**修正：台積電損益顯示0的bug（使用者：「今日股價已上升，損益不應為0」）**：直接查證yfinance原始資料，發現08-11那天K棒**Close/High/Low/Open全部是NaN**（只有成交量16,787,063是真的）——這是yfinance資料源本身對台股的回補延遲，不是我們的bug。但既有程式碼用`hist['Close'].notna()`濾掉NaN列後，會靜靜地退回顯示前一個交易日(08-10)的收盤價(2380，剛好等於這筆持倉的成本價，才會讓損益算出0)，卻沒有任何提示這其實是舊資料。查證yfinance的`fast_info`（近即時報價，跟歷史K棒`.history()`是不同管道）能正確回傳2395（跟使用者說的一致）。修正`fetch_yfinance_history`：當歷史K棒最新一列的Close是NaN時，改用`fast_info.last_price`（連同day_high/day_low/open）補上這一列。這個修正對所有股票、所有呼叫端（首頁掃描、操盤室、下單建議、sell_check）都生效，不只是台積電或`/api/stock/{ticker}`單一端點。實測確認`/api/stock/2330`修正後正確回傳2395。

**研究：「連續同買天數」——使用者直覺 vs 嚴謹交叉驗證，結論被推翻**

使用者問：模型是否優先推薦「外資投信同買第一天」的標的（越早進場越好），並要求做嚴謹研究。

*第一版分析（有嚴重方法論瑕疵，僅供對照）*：把全部677組實驗（含grid trial）的514,881筆交易全部pool在一起，依進場當下的「連續同買天數」分桶算平均報酬，結果顯示「同買第1天」平均報酬1.22%、全部天數裡最高，看起來支持使用者直覺。

*第二版分析（修正方法論後，結論完全不同）*：
1. **關鍵瑕疵**：677組實驗只是同一份5.5年歷史資料的677種不同參數排列組合（max_positions/hold_days/exit_strategy不同），同一個真實訊號（同一檔股票同一天）會被好幾百種參數組合各自「買一次」，重複計入。用唯一`(ticker, buy_date)`去重後，樣本從514,735筆暴減到**只剩3,459筆真實訊號**（放大倍數約148倍！），去重後排序完全改變：同買第2天(0.31%)平均報酬反而最高，第1天只有0.15%，且從第3天起轉負——原本「第1天最好」的結論主要是重複計數的假象。
2. 依策略拆開看（未去重版），A/B/C/D/E五個方案各自的「最佳進場天數」不一致（A方案第1天最好，B方案第6天以上最好但樣本小，D方案第4~5天勝率最高）。
3. 依年度拆開看更明顯不穩健：2021/2024年第1天表現較好，但**2022空頭年反而是同買第6天以上表現最好**（平均+0.76%、勝率55.8%，遠贏第1天的-0.76%、勝率28.7%），2025/2026年則是第4~5天最好。「越早越好」這個方向在不同年份甚至會整個反過來。

*第三步（決定性驗證）：真的把「限定進場天數」寫成濾網，跑完整投資組合回測*，用strategy A、8檔持股、5.5年全期間，四組對照（跟case_studies個案⑥的方案A基準314.29%用同一組參數設定比較基礎）：

| 濾網 | 總報酬 | MDD | 勝率 | 獲利因子 | 交易數 |
|---|---|---|---|---|---|
| **不限制(現行系統真實行為)** | **232.06%** | 30.57% | 42.59% | **1.58** | 965 |
| 只准同買第1~2天進場 | 211.02% | 35.30%（更差） | 40.21%（更差） | 1.43 | 950 |
| 只准同買第1天進場 | 220.74% | 27.11%（較佳） | 39.67%（更差） | 1.35 | 915 |
| 只准同買第3天以上進場(反向對照) | 97.81%（大幅更差） | 39.02% | 41.40% | 1.35 | 314 |

**最終結論：使用者的直覺方向不成立，不建議把「連續同買天數」做成硬性進場濾網**。不限制（現行系統的真實行為）在總報酬與獲利因子上都是四組裡最好的；限定只准第1天或第1~2天進場，總報酬反而變差（211~221% vs 232%），只有MDD在「只准第1天」版本略為改善。完全排除頭兩天（反向對照組）報酬大幅惡化且交易數腰斬（機會成本太高）。這個結論延續2026-08-10已有的發現模式（乖離率/動能類指標不該當硬性濾網），「同買連續天數」也加入這個名單。

**方法論教訓（新增）**：`backtest_logs.db`裡677組實驗共用同一份歷史資料，任何要用`trades`表做per-trade統計分析的研究，都必須先確認樣本是否被「同一訊號被多組實驗重複買進」嚴重放大，去重後結論可能完全相反——這次的第一版分析正是活生生的示範案例。

已存檔：`cobuy_A_overall_formal.csv`（正式實驗全樣本分桶）、`cobuy_B_dedup_unique_signal.csv`（唯一訊號去重版）、`cobuy_formal_trades_full.csv`（逐筆明細）、`cobuy_streak_backtest_results.csv`（四組回測總表），皆在scratchpad，尚未整理進`research_data/`或個案研究網頁（此為探索性研究，結論是「不採用」，暫不列入正式個案研究，僅記錄於此供之後查閱）。

**目前狀態**：這次的bug修正（`main.py`的市況警語合併邏輯、`chip_cache.json`架構、`fetch_yfinance_history`的NaN補值、`portfolio.json`的6213 buy_date）都尚未commit。「連續同買天數」研究純屬回溯分析+獨立回測腳本，沒有修改任何正式程式碼，`strategy_core.py`/`chip_score`公式維持原樣。

**補充驗證：regime-conditional假設也不成立（樣本量不足）**：使用者提出合理延伸假設——連續同買天數的最佳解會不會跟Regime自動切換一樣，隨多空市況而不同？依年度拆開的表格看起來像有線索（2022空頭年同買第6天以上表現最好）。但查證後發現：那張「依年度拆開」表用的是**未去重**的514,735筆資料，犯了跟第一版分析同樣的重複計數錯誤。改用去重後的唯一訊號重新拆解2022年，「同買第6天以上」真正獨立的訊號只剩9筆，報酬轉為-0.28%（原本以為的+0.76%/勝率55.8%消失），根本不是「表現最好」。

根本問題是樣本量：126檔股票、5.5年，去重後同買訊號一年約550~680筆，再切6個天數分桶×6個年度，每格平均只剩幾十筆，部分格甚至個位數（2021年第6天以上n=4、2022年n=9、2025年n=5）——這個粒度不足以支撐任何regime-conditional細分規則，不是規律不存在，是現有樣本量還無法可靠分辨真規律與巧合。這跟Regime自動切換用的TAIEX訊號（市場級、每個regime狀態下數千筆交易可驗證）完全不是同一個資料量級。

使用者認同「穩健核心+動態調整的靈活參數」這個設計哲學本身沒錯（Regime自動切換就是成功案例），只是這個特定變數（連續同買天數）目前資料不夠支撐，要求**記錄下來、以後提醒使用者**——待未來歷史資料/股票池累積更多獨立訊號後，值得重新評估連續同買天數是否適合做成regime-conditional規則（或軟性加權，而非硬性濾網）。已存檔`case9_cobuy_dedup_by_year.csv`並補寫進`case_studies.html`個案⑨。已存為memory提醒（見`project_cobuy_streak_reminder.md`）。

**已完成commit**：本節（含Stage2/3上線、四個bug修復、個案⑨初版+這次補充驗證）已於同一批commit內完成，見git log。

## 個案⑩：「大買」（買超金額大小）該不該加強推薦訊號——四種機制全部驗證失敗

使用者問：目前`calculate_chip_score`只判斷外資/投信買超正負，不管金額大小，「大買」該不該加強推薦訊號？並要求：①用網路可查證的專業定義（查證後確認台股沒有固定金額的「大買」業界標準，改用相對統計門檻：買超金額排進該股過去60日前10%/25%，或佔當日成交量比例達90th/75th百分位，門檻依實際資料分布校準）；②交叉測外資/投信不同權重；③把個案⑨的連買天數也納入交叉分析。使用者要求先討論定義再動手，且要求結果不管好壞都整理成正式個案研究記錄。

**第一輪（大買 vs 一般同買）**：per-trade去重分析顯示「一般同買（未達大買門檻）」平均報酬是負的，比完全沒有同買訊號還差，只有「大買」才優於平均——比個案⑨的天數變數更有分離度，逐年拆開4/6年方向一致（2021反轉、2023混雜）。但決定性投資組合回測（strategy A、8檔、5.5年）：baseline 232.06%/MDD30.57%/PF1.58，硬性濾網寬鬆版199.68%、嚴格版146.55%、**連軟性加權版（不排除訊號，只加分排序）都只有231.83%且MDD更差（34.03% vs 30.57%）**——四組全部不如baseline。

**第二輪（外資/投信權重篩選）**：測5種權重組合（等權重/只看投信/只看外資/投信加重/外資加重），per-trade快篩發現「只看外資」分離度最好（大買-一般同買報酬差距0.601，勝率46.8%最高），意外不是市場常識認為對中小型股更有指標性的投信。

**第三輪（連買天數×大買交叉分析）**：發現真正的交互作用——大買在同買第1~2天有明顯幫助（合計n=1,311，兩天報酬都轉正），但第3天反轉（大買反而比一般同買差），第4~5天兩者都差，不是單調關係。

**最終決定性驗證**：把第二、三輪篩出的最強候選（只看外資前25%；大買+限定第1~2天）做完整投資組合回測——只看外資版205.54%/MDD37.08%（更差）；大買+前兩天組合版195.83%/MDD32.78%（也更差）。**即使是三輪篩選出來最有希望的候選條件，投資組合層級依然打不贏baseline。**

**結論**：「大買」不該加強推薦訊號，不管是硬性濾網、軟性加權、外資/投信權重調整、或跟連買天數交叉的組合條件，全部驗證失敗，延續個案⑨的教訓——per-trade優勢不會自動轉換成投資組合層級優勢，任何候選規則都必須走完per-trade→逐年穩健性→真實投資組合回測三層驗證才能下結論。已整理成`case_studies.html`個案⑩，含四輪完整過程與資料連結，不管結果好壞都如實記錄。

已存檔：`case10_bigbuy_dedup_strict/loose.csv`、`case10_bigbuy_backtest_4way.csv`、`case10_bigbuy_weighting_screen.csv`、`case10_bigbuy_streak_cross.csv`、`case10_bigbuy_final_backtest.csv`，皆在`research_data/`並連結進個案⑩。

**目前狀態**：`case_studies.html`、`research_data/README.md`、`main.py`（白名單）均已修改但尚未commit。純研究性質，沒有修改`strategy_core.py`/`chip_score`公式。

## 個案⑪：「大賣」該不該加強出場訊號——事件研究+逐年穩健性自我修正

延續個案⑩，使用者問對稱問題：方案B的「土洋雙賣」出場規則目前只看正負號，該不該用賣超金額大小加強？改用事件研究法（不用backtest_logs.db的trades表，避免677組實驗重複計數問題）：對126檔股票5.5年，找出每個「同賣日」，直接看往後5/10/20日股價實際報酬，比較大賣vs一般同賣。

**事件研究結果**：24,975個同賣事件，所有分類（一般同賣/大賣前25%/大賣前10%）後續報酬全部是正的，且賣超金額大小跟後續表現沒有單調關係——大賣（前10%）後續表現甚至跟一般同賣差不多，中間的前25%反而最弱。訊號比個案⑩的大買更弱。

**外資/投信權重篩選**：5種權重組合，大賣vs一般同賣的10日後續報酬差距全部在±0.2%以內，沒有明確訊號（大買研究裡最好的權重差距有0.6%，這次遠不如）。

**連賣天數×大賣交叉分析**：佔樣本85%以上的第1~2天完全看不出大賣的額外影響；第3天、第4~5天有一點「大賣後續較弱」跡象但樣本已經變小（合計2,929筆），第6天以上又反轉。

**決定性驗證（5.5年）**：把方案B「土洋雙賣」改成要求大賣(前25%)才觸發，總報酬從baseline的161.06%跳到**279.22%**，MDD從36.97%改善到30.83%，勝率43%→48%，看起來是本系列研究裡唯一的正面結果。

**逐年穩健性檢查，推翻上面的結果**：2022空頭年baseline -20.96%/MDD24.36% vs 大賣濾網版-26.40%/MDD30.61%（更差）；2025年baseline 61.72%/MDD22.59% vs 大賣濾網版43.77%/MDD28.54%（更差）；只有2024年報酬較好但MDD也更差。**三年裡兩年都更差**，5.5年連續回測看起來的巨大優勢沒有在任何單一年度穩定重現，是特定連續期間複利路徑造成的假象——這正是個案⑤已經記錄過的「連續回測分段謬誤」，這次在自己的正面結果裡又踩了一次，立即自我修正。

**結論**：「大賣」不該加強出場訊號，方案B現行「土洋雙賣只看正負號」維持不變。已整理成`case_studies.html`個案⑪，如實記錄「5.5年回測一度看似大幅改善、逐年檢查後推翻」的完整自我修正過程。

已存檔：`case11_bigsell_event_summary.csv`、`case11_bigsell_weighting_screen.csv`、`case11_bigsell_streak_cross.csv`、`case11_bigsell_backtest_5.5yr.csv`、`case11_bigsell_year_robustness.csv`，皆在`research_data/`並連結進個案⑪。

**附帶查證：6531愛普的實際買入時機**：使用者問6531連續3天(08-07/08-09/08-10)排名第2、chip_score=5「土洋同買」，08-10買入是否符合個案⑨「第3天容易虧損」的發現？查證`history.json`裡6531真實的法人買賣資料（比scan時的即時快照更完整/後續已確認的版本）：08-06 foreign=205/trust=46(同買，streak=1)、08-07 foreign=26/trust=81(同買，streak=2)，**但08-10當天實際上foreign=-475（外資淨賣超），trust=+1**——不符合`is_cobuy`定義（foreign>0且trust>0），也就是說08-10這天外資其實已經在賣，不是「同買第3天」，掃描結果顯示的「🔥土洋同買」訊號很可能是抓到08-07當天的資料當作「今天」（FinMind的法人資料通常要等收盤後才會公布，盤中/當天掃描抓到的常常是前一個已公布交易日的數字），代表使用者買進的時間點，實際的法人動向已經反轉，不是模型呈現的「延續中的同買」。這是一個比個案⑨的「第3天」更值得注意的情況——不是規律本身的問題，是即時掃描顯示的訊號可能落後於真實最新籌碼狀態一天。

**附帶查證：操盤室P&L儀表板數字驗證**：使用者移除6531後，未實現損益從+32,400→+47,000，已實現損益0→-14,600，總戰果維持+32,400不變，問這樣對不對。驗證：移除前未實現損益+32,400 = 2330(+10,500)+6213(+36,500)+6531(-14,600)；移除後未實現+47,000 = 2330(+10,500)+6213(+36,500)（6531移出這個桶）；已實現-14,600就是6531的損益轉移過去。數字完全吻合，機制正確——移除操作只是把一筆部位從「未實現」搬到「已實現」，不會憑空改變總和。但查證`history.js`的`removeFromPortfolio`函式，`pnl = exitPrice - item.cost`是**原始價差，沒有扣除交易手續費/證交稅**（不像回測引擎統一用0.3%單邊的「真實嚴苛」費率）——已跟使用者說明，真實已實現損益扣掉手續費後會比顯示的-14,600更差一點。

## 個案⑬：三大法人資料未來函數——整個系統的關鍵修正

使用者延伸6531的觀察，提出一個尖銳假設：案例⑨「第3天容易虧損」的發現，會不會是因為系統顯示的「今天同買」訊號其實是延遲資料（因為當天法人買賣超還沒公布），導致回測標記的「第N天」換算成真實世界能行動的時間點已經是延遲後的更晚時機？並特別強調：晚一天知道「買」可能還好（主力通常會抱一陣子），但晚一天知道「賣」很危險（籌碼已經轉弱卻還在抱）。

**查證公開資訊**：證交所每檔個股三大法人買賣超要收盤(13:30)後**下午5點**才公布；FinMind要**隔天凌晨1:30**才整理進API。T日的法人資料最早T日17:00才存在，實際能查到要等T+1凌晨。

**查code，確認整個系統確實存在未來函數**：`strategy_core.py`本身乾淨（只接收資料），問題在4個呼叫端：
1. `backtest_engine.py`出場迴圈：用`cdf.loc[current_date]`（當天）判斷當天出場，執行價也是當天收盤。
2. `backtest_engine.py`進場候選篩選：`inst_list`用`[idx-1, idx]`（含當天）餵給`calculate_chip_score`，其取`inst_last2_days[-1]`當「今天」，等於當天資料才是真正驅動分數的那筆。
3. `main.py`的`_simulate_holding_exit_signal`（操盤室賣出提醒的回放邏輯）：同樣用`chip_df.loc[current_date]`。
4. `main.py`的`run_scan`/`process_ticker`（首頁即時掃描）：靠FinMind本身的發布延遲「湊巧」不出包，沒有明確防呆，風險較隱性但仍該修。

**修正**：全部改用「前一個有資料的交易日」（`idx-1`取代`idx`，entry用`[idx-2, idx-1]`取代`[idx-1, idx]`），執行價維持當天收盤不變（這部分合理：開盤前就已知道前一天17:00公布的資料）；首頁掃描額外加上明確日期防呆(`inst_data`過濾掉日期>=最新股價交易日的列)。已測試main.py/backtest_engine.py都能正常import跟執行，本地伺服器重啟後正常運作。

**重新驗證：Regime研究關鍵數字全部下修，且冠軍配置改變**：
- 個案⑤baseline：522.18%(MDD22.25%) → **409.67%(MDD24.45%)**
- 個案⑦土洋雙賣疊加版：468.71%(MDD17.48%) → **359.34%(MDD27.59%，明顯惡化)**
- 個案⑦ 48組窮舉冠軍：692.64%(MDD24.17%，含土洋雙賣) → **651.37%(MDD38.26%，冠軍配置改變——最佳解拿掉了土洋雙賣**，原冠軍配置修正後降到第7名408.07%)

冠軍配置真的變了，不只是數字下修——這正好呼應個案⑪的獨立發現：土洋雙賣訊號本身用未來函數算，優勢有一部分是「偷看」出來的。三個獨立研究方向（案例⑨連買天數、案例⑪大賣、這次的未來函數修正）最後匯聚到同一個結論：籌碼類的「當天訊號」普遍沒有想像中可靠。

**使用者接著問了另一個延伸問題**：市場傳言投信因法規不能大買大賣當沖、買超較持久，外資（尤其「假外資」）較不穩定——用既有籌碼資料庫驗證，結果整理成**個案⑫**：投信連續買超streak確實系統性比外資長（平均2.94天vs2.20天）；外資買超後99.6%會在20天內反手賣出、平均只要2.53天（投信94.4%/4.92天）；但後續報酬兩者差不多，外資甚至略高——「快進快出」不等於「訊號差」。這個發現也支持了案例⑬「出場端修正更優先」的判斷：外資訊號反手中位數只要2天，1天延遲的相對衝擊遠大於投信（中位數3天才反手）。

**已完成commit的部分**：`strategy_core.py`的呼叫端修正（`main.py`、`backtest_engine.py`）、`case_studies.html`個案⑫⑬、`research_data/README.md`、白名單。

**`backtest_logs.db`全面重跑已完成**：810組正式回測實驗（大數據回測量化分析報告、個案⑨⑩⑪引用的全部歷史交易）原本全是修正前產生的。備份舊資料庫（`backtest_logs_BACKUP_pre_lookahead_fix.db`）後清空重跑，811組全部以修正後邏輯重新產生，0失敗。

## 2026-08-13：個案⑨⑩⑪三案全部改用修正後邏輯重新驗證（收尾個案⑬的未來函數修正）

把個案⑬發現的未來函數修正，同樣套用到個案⑨（連買天數）、⑩（大買金額）、⑪（大賣金額）三個獨立研究腳本，重新產生完整投資組合回測，逐一更新`case_studies.html`對應章節：

**個案⑨（連買天數）**：決定性回測baseline從232.06%下修為**215.49%**（跟個案⑩⑪新baseline一致，三案現在共用同一個修正後基準）；MDD從30.57%上修到33.03%。原本「同買第3天以上」反向對照組是+97.81%（比baseline好），修正後**反轉為-10.12%（比baseline大幅差、MDD惡化到61.25%）**——這個反轉本身就是未來函數的直接證據：舊邏輯下「第3天」訊號其實偷看了未公布的當天資料，去重後per-trade分析也同步發現最佳持有天數從「第2天」變成「第1天」（更乾淨的單調遞減）。決定性結論不變：不建議把連續同買天數做成硬性濾網。

**個案⑩（大買金額）**：兩輪決定性回測（4種硬濾網/軟加權/權重調整組合 + 2個交叉分析篩出的候選）全部下修，但**排序結構完全沒變、結論不變**——baseline（215.49%）依然贏過所有測試過的濾網/加權版本（loose 187.03%、strict 154.81%、soft_boost 196.65%、foreign_only 140.47%、equal+day1-2 171.00%）。這是三案裡對修正最「無感」的一個，代表這條研究線本來就沒有被未來函數污染太多。

**個案⑪（大賣金額）**：決定性回測baseline 168.71%、大賣濾網(前25%) 246.52%（仍強烈正向）、(前10%) 179.19%——修正後數字下修但方向不變。**逐年穩健性檢查本身也被回頭檢查**：原本只查了2022/2024/2025三年就下「推翻」結論，這次補齊2021、2023兩年後（改用全5年），發現證據其實是**混雜**的——5年裡3年（2021、2023、2024）大賣濾網報酬較好，2年（2022空頭年、2025）較差，且唯一的空頭年樣本（2022）明確不利。結論從「清楚反證，不要用」修正為「證據混雜，傾向保守維持現狀，但比⑨⑩更值得繼續觀察」。**方法論教訓的教訓**：連「逐年穩健性檢查」這個用來防止過度樂觀的驗證步驟，自己也可能因為取樣不全（只挑幾年）而產生誤導性結論——驗證步驟本身也需要被覆核，不是加了一層檢查就自動可信。

**三案交叉比對，回答「未來函數修正對哪類研究影響最大」**：影響程度跟「該訊號本身跟『連續性/當天狀態』綁得多緊」成正比——⑨（連買天數，直接依賴逐日累積的streak計算）反轉最劇烈；⑪（大賣，出場端土洋雙賣本身就是當天判斷觸發）次之；⑩（大買金額，本質是「這筆訊號的分數該不該加權」，跟哪一天無關）幾乎無感。這跟個案⑬本身「土洋雙賣（出場、當天觸發型）比乖離率/動能類（進場、非當天觸發型）受影響更大」的發現一致。

**待辦**：`analysis.html`大數據排行榜的811組新結果目前僅資料庫層完成，頁面本身依賴即時查詢所以會自動顯示新數字，已在瀏覽器驗證確認（811組實驗、top return 384.18%）。R策略（個案⑤⑦⑧原本考慮上線的配置）新的48組窮舉冠軍（651.37%/MDD38.26%，拿掉土洋雙賣）**尚未寫回`strategy_core.py`**——這是使用者明確要求「等研究做得夠透徹後提醒討論」的項目，尚未詢問，見下方待辦提醒。

## 2026-08-13（續）：Regime研究全面更新（analysis.html／research_report.html／case_studies.html），發現核心結論反轉

使用者要求「大數據回測量化分析報告，其相關網頁及文章圖表也都要隨著我們的研究報告出來後要新增或更新替換」，把個案⑬的未來函數修正推廣到`analysis.html`第六節、`research_report.html`全文、`case_studies.html`個案⑤⑦⑧。過程中發現一個之前遺漏的問題與一個比預期更嚴重的結論反轉：

**遺漏的問題**：`regime_bear_exhaustive_search.py`這支腳本本身永遠套用巨觀風控（`macro_series.get(...).get("veto_buy")`），沒有獨立的fail-open變體。先前case⑬文件裡「692.64%（修正前fail-open）→651.37%（修正後）」的對照，其實是拿修正前的fail-open數字去對比修正後的風控生效數字，是同時疊加了兩種修正（look-ahead修正+風控狀態不同）的不公平比較。已寫一個強制關閉風控否決的fail-open變體腳本（`regime_bear_exhaustive_search_failopen.py`）重新窮舉48組，補上修正後的fail-open champion：**671.27%/MDD 47.84%**（配置：防線A/B/高點鎖利/保本停利/土洋雙賣全關，幾乎不設任何出場規則）。MDD 47.84%極高，本質是「拿極端風險換報酬」的解，不是穩健規則設計的結果。

**更嚴重的發現：重跑`regime_adaptive_backtest.py`（產出個案⑤三欄對照表的腳本）發現它的entry端`inst_list`同樣是`[idx-1, idx]`（含當天）的舊pattern，之前遺漏未修正**。修正並重跑後：

| 方案 | 修正前 | 修正後 |
|---|---|---|
| 靜態基準（一次買完+卡片四防線） | 298.72% | 254.74%（MDD 29.82%） |
| 靜態最佳單一改動（一次買完+關防線A） | 394.38% | **425.17%（MDD 24.14%，現在三組裡最高、MDD最低）** |
| Regime自動切換 | **522.18%（原本最高）** | 409.67%（MDD 24.45%，**退居第二**） |

**核心結論反轉**：修正前Regime自動切換領先靜態最佳單一改動約33%；修正後Regime自動切換反而被這個完全不需要regime判斷的極簡靜態方案反超（且MDD更高）。樣本外年份檢查也一併重跑：2021從原本正報酬（+139,412）轉為負報酬（-114,456），2022勝率進一步下修到33.3%（原40.5%）。

**48組窮舉的交叉驗證結論也一併修正**：風控生效版新冠軍651.37%/MDD38.26%（防線全關、保本adjust、**土洋雙賣關**），舊冠軍配置（含土洋雙賣）修正後在風控生效版只排第8名（407.30%，非先前記錄的第7名/408.07%，這次是用乾淨重跑的資料重新計算，取代先前可能有汙染的估計值）。「兩種風控狀態下冠軍配置完全相同」這個個案⑦原本信心最高的穩健性論證，在此次修正後不再成立（新冠軍在fail-open與風控生效兩版的細節配置略有不同，但「不含土洋雙賣」這一點是一致的）。

**已完成的頁面更新**：`case_studies.html`個案⑤（三欄表+樣本外年度表+結論全部更新，含反轉警示）、個案⑦（fullswap分支標註未重跑；土洋雙賣疊加版與48組窮舉+交叉驗證全部更新為修正後數字與新冠軍配置）、個案⑧總結（Step 3/6/7/8+最終配置表+驗證程度局限性列表全部反映反轉）、個案⑬的修正前後對照表（改為同口徑fail-open vs fail-open、風控生效vs風控生效的公平比較）；`research_report.html`（新增頂部重大修正通知、3.4/3.6/6節更新、新增4.5未來函數方法論教訓）；`analysis.html`第六節（KPI卡改為修正後三組數字，insight-card改寫）。`research_data/`：`case5_regime_v1_trades.csv`（297筆）、`case7_strategyB_dualsell_overlay_trades.csv`（471筆）、`case7_bear_exhaustive_48combos.csv`／`_macroON.csv`（48列，修正後排序）全部重新產生並覆蓋舊檔；`README.md`同步更新描述。

**目前狀態**：Regime自動切換研究線的結論從「已驗證優於固定方案，考慮正式導入」變成「目前不建議導入，需要重新設計regime子規則來源」——`strategy_core.py`維持不寫入R策略新配置，這個決定在此次修正後反而更站得住腳，不需要再等使用者確認「要不要導入」（因為已經不是「導入哪個更好的版本」的問題，而是「目前這條研究線還沒有一個比簡單方案更好的版本」）。[[project_regime_adaptive_promotion_reminder]] 這個提醒記憶需要同步更新，反映此次反轉。

## 2026-08-13（續2）：個案⑭——方案E補齊正式驗證，逐年拆解後是條件式優勢不是全面升級

使用者問「方案E為何不能納入分析報告中？我覺得應該可以」。查證發現方案E（`strategy_core.py`已完整實作：土洋雙賣觸發時依賺賠分流，虧損中比照方案B立即出場，獲利中改收緊移動停損而非直接出清）不是程式碼擋住它，是`backtest.html`大數據回測勾選框預設沒勾選E，導致正式資料庫裡只有12組修正未來函數前的舊E實驗，樣本量遠不如A/B/C/D各自的~200組，無法公平比較。

**第一步，補齊資料**：刪除12組舊E實驗，複製方案D的199組正式參數組合改用E重新跑一輪（直接呼叫`run_backtest`，同步存入SQLite），199組全部成功。結果：方案E直接包辦排行榜前5名（388.58%~406.11%），原冠軍方案D（384.18%）退到第6名。用production實際的8檔持股重新比較五方案最佳設定：A 268.86%／B 203.71%／C 227.84%／D 278.78%／**E 299.26%(MDD 25.93%，五方案裡最低)**——不是只有極端集中持股（2~3檔）才成立的排行榜偏誤，8檔設定下E依然報酬最高、風險最低。

**第二步，比照個案⑥方法論做逐年拆解**（8檔持股，E用30天、D用20天、A用90天，各自的8檔最佳持有天數），發現排行榜的5.5年連續優勢**不是每年都站得住腳**：2021、2024兩個多頭年，方案E反而是三組（A/D/E）裡表現最差的（2021：E -0.69% vs D +15.83%；2024：E +27.40% vs A +46.77%）；2022空頭年、2023方案E最好（2022唯一正報酬+1.68%；2023 +66.33%）。6年裡A/D/E各贏2年，沒有任何一個方案「每年都最好」。5.5年累積的299.26%領先，是複利路徑的結果——E在對的年份（空頭/整理）贏得夠多，蓋過了它在強勢多頭年落後的部分。

**第三步，出場原因拆解找機制**：查836筆交易（8檔/30天版本）的出場原因分布，發現方案E設計理念強調的「獲利中收緊停損、給獲利繼續奔跑的空間」這個招牌機制，作為獨立標籤的出場原因（雙賣後鎖利出場）只出現5次（0.6%）——幾乎沒被單獨觸發過，因為多數獲利部位在真正觸發收緊停損之前，早就已經先達到15%停利出場了。真正跟方案B有差異化、且佔比達37%的是「虧損中的土洋雙賣」：平均-4.56%、平均只抱8.3天，比同樣是停損類的「觸發停損」（平均-5.40%、抱12.3天）更早、更輕地認賠出場。**方案E的優勢比較可能來自「虧損時比其他方案更快認賠」，不是原始設計意圖強調的「獲利時給空間繼續漲」**。

**跟Regime研究線（個案⑤⑦⑧）的關鍵差異**：這次逐年拆解沒有推翻結論，是把「全面優於現行方案」修正為「有條件的優於現行方案」——不是bug造成的假象，是可以用交易機制解釋、真實存在但有適用市況的風險輪廓（偏防禦、偏空頭/盤整）。同一套「連續回測優勢不代表逐年都站得住腳」的方法論教訓，這次得出的是需要調整使用情境的結論，不是整條研究線作廢的結論。

**已完成的頁面更新**：`case_studies.html`新增個案⑭（補齊網格、逐年拆解表、出場原因拆解表、結論與實戰建議）；`backtest.html`方案E勾選框改為預設勾選、下拉選單移除「實驗性」標籤；`analysis.html`第二節方法論新增方案E說明、`doc.html` Q13更新為「5大既有方案(A~E)」。`research_data/`新增`case14_leaderboard_top10.csv`、`case14_strategyE_year_isolation.csv`、`case14_strategyE_exit_reason_breakdown.csv`，`README.md`同步更新。

**目前狀態**：方案E值得作為「偏防禦」的可選方案提供給使用者（類似方案B/C的定位），特別適合預期空頭或盤整市況；**不建議直接取代方案D成為系統下拉選單的預設值**，因為強勢多頭年方案D和方案A表現明顯更好。`backtest.html`的<code>btExitStrategy</code>下拉選單預設值維持方案D，不會因為這次研究自動切換。

## 2026-08-13（續3）：個案⑮——「智慧推薦」可行性驗證，12組regime切換設計全數輸給全程用方案E

使用者延伸個案⑭的發現，明確提出核心訴求：「本來就是希望模型能因應不同股市狀況做買賣推薦，在何種趨勢用何種方案才是我要的重要答案」「沒有萬能最佳解的方案，只有適應趨勢的最佳解」——希望做出下單前的智慧推薦。這比個案⑤⑦⑧原本嘗試的「重新設計組合出場規則」更保守：不自創規則，只是在進場當下依regime訊號「整包選用」哪一個已驗證方案（A/B/D/E），之後這筆持倉固定沿用該方案的完整規則到出場。

**Part 1，原始20日均線訊號**：8檔持股、持有天數統一固定30天（隔離「選哪個方案」這個變數本身），全期間+逐年獨立回測。三組配對（多頭A/空頭E、多頭A/空頭B、多頭D/空頭E）**全部輸給「全程只用方案E」（286.64%/MDD26.19%）**，且MDD全部更高。

**使用者追問**：「你的多頭空頭是以一年為單位，能夠再縮短到一至三個月的範圍來判定？」——查證發現這個假設反了：regime訊號本身早就遠比1~3個月敏感，5.5年內翻轉141次，維持同一狀態的天數中位數只有4個交易日、平均9.6天，2022一年就翻轉21次。問題不是訊號太慢，是太容易來回翻轉，導致新進場部位常被誤配到「短暫翻轉、很快又轉回去」的錯誤方案。

**Part 2，拉長平滑訊號重測**：使用者授權「你想到的都應該仔細去做，不用再問，最好一次都做完」，測試3種平滑方式（60日均線、20MA+連續5天確認、20MA+連續10天確認，翻轉次數分別降到70/50/32次）× 3組方案配對 = 9組全期間回測。**結果：9組依然全部輸給全程只用方案E**，且加重平滑（confirm10）反而讓部分配置更差（176.41%/158.69%，MDD升到41.53%/44.09%，是全部12組切換版本裡最差的兩組）——平滑降低了翻轉次數，但確認延遲讓部位在真正轉折點時錯配更久，抵銷了翻轉次數減少的好處。

**核心結論**：原始+3種平滑訊號×3種方案配對，總共12組「regime判斷後切換整包方案」的設計全部失敗。這不是bug造成的假象（跟個案⑤⑦⑧不同），是切換這個動作本身在這組資料上就是負值——因為方案E的土洋雙賣快速停損機制，本身就是「當下籌碼真的轉弱就立即反應」的<strong>反應式</strong>設計，不需要準確預測regime轉換就能自然在空頭/盤整期少虧、多頭期跟上。使用者「適應趨勢的最佳解」這個訴求，實際上已經被方案E的設計滿足，只是不是用「判斷regime→換方案」這種外顯方式。

**對「下單前智慧推薦」功能的具體建議（尚未實作，待使用者確認方向）**：
1. 不建議做成「依regime自動切換使用哪個整包方案」的推薦邏輯——12組設計全部驗證失敗。
2. 建議改成「方案本身特性」的推薦：下單建議頁面直接呈現「方案E目前是全期間實測報酬最高、風險最低的方案」這個結論，讓使用者自行決定要不要採用，而非系統自動判斷市況幫使用者選。
3. 若仍想顯示regime資訊，可保留首頁「研究版advisory面板」當作純資訊性參考（不驅動推薦邏輯）。

**已完成的頁面更新**：`case_studies.html`新增個案⑮（Part 1原始訊號失敗、Part 2三種平滑訊號依然失敗、結論與對「智慧推薦」功能的具體建議）；`research_data/`新增`case15_regime_strategy_selector.csv`、`case15_regime_strategy_selector_smoothed.csv`，`README.md`同步更新。**尚未修改任何正式程式碼**——這輪全部是研究驗證，使用者明確要求「先討論」才動手做UI/推薦功能本身，目前應等待使用者確認要採用上述哪個方向再繼續。

## 2026-08-13（續4）：使用者確認「不刪除⑤項待辦，依優先順序做完」，執行①②③⑤四項

使用者對個案⑮結論的具體落地方式回答「同意」（方案E設預設值、下單頁保留A/B/C/D/E手動選單、首頁regime面板維持資訊性），實作見下方獨立commit。接著問起`strategy_discussion.html`「五、後續討論串」的五個舊待辦是否還要做——盤點後確認全部未完成、沒有一項因為今天的研究而變得多餘，且①③兩項優先順序因為今天的結果而提高。使用者指示「既然不該刪去，那為何我們不繼續」+「依照輕重緩急對我們模型有益的優先程度完成所有研究與計劃」，於是依序執行：

**①`get_attribution()`重寫（已完成）**：`backtest_engine.py`的`/api/analysis/attribution`原本寫死比較方案D跟C（"策略方案抉擇"洞察文字固定算`strat_rets.get('D') - strat_rets.get('C')`），方案E補齊後這個寫死比較對象本身就是錯的。改成動態找出`df_exp.groupby('exit_strategy')['total_return'].mean()`裡實際的第一名/第二名，同時報告MDD最低的方案（若跟報酬冠軍不同，明確標示「是取捨不是全面最優」）。同時把持倉檔數建議（原本寫死比較3檔vs5檔的MDD）也改成動態抓「MDD最低的實際檔數」vs「檔數最少的基準」。重啟服務後驗證：目前全網格平均下方案A報酬最高（218.95%），方案E次高（205.57%）但MDD最低（39.79%），持倉建議正確抓到8檔。

**②Tier 3（Sharpe/Sortino，已完成）**：`experiments`表新增`sharpe_ratio`／`sortino_ratio`兩個REAL欄位；`run_backtest()`在算完MDD後，直接用已經在算的逐日equity算逐日報酬率，年化Sharpe = `mean(daily_returns)/std(daily_returns)*sqrt(252)`、Sortino同公式但分母只取下檔波動，無風險利率簡化為0（不用額外資料來源，本機免費yfinance股價已足夠）。備份舊DB（`backtest_logs_BACKUP_pre_sharpe_sortino.db`）、刪除1016組正式實驗（提取後發現實際筆數是1016，不是先前記錄的998，中間累積了order_planner測試等產生的少量新實驗）、全部重跑回填。`analysis.html`排行榜表格新增Sharpe/Sortino兩欄（≥1標綠）。

**③Optuna系統化參數搜尋（已完成，發現本輪最重要的正面結果）**：把方案E的5個寫死出場常數（進場ATR倍數3.0、鎖利觸發12%、鎖利後ATR倍數1.25、固定停利15%、雙賣仍獲利收緊ATR倍數1.0）變成連續參數，用Optuna在訓練期(2021-01-01~2024-11-01)跑50個trial搜尋（評分公式：總報酬-2×MDD，跟既有grid_search端點一致），測試期(2024-11-02~2026-08-10)驗證。**這是今天唯一一個通過樣本外驗證、真正打敗預設值的優化**：全期間286.64%→311.72%，MDD同時從26.19%降到24.45%；測試期（樣本外）99.35%→110.94%，也是正向。找到的最佳參數方向跟個案⑭「E的招牌機制(獲利中收緊停損)幾乎沒被單獨觸發過」互相呼應——Optuna自己把這個機制的收緊倍數從1.0放寬到1.62，同時把固定停利上限從15%拉高到18.7%，方向是「快速停損保留，但放寬對獲利部位的干預」。完整記錄見 <a href="case_studies.html#case16">個案⑯</a>。

**③續（同日稍後）：使用者同意採用建議，正式寫入`strategy_core.py`**。先比照個案⑭方法論做逐年獨立回測（研究腳本簡化版）：6年裡3勝3負，贏的margin較大（2021/2023/2025），輸的margin較小（2022/2024/2026，除了2026年MDD明顯變差3.66個百分點需留意，但2026只有7.5個月資料）。接著用**正式`run_backtest`引擎**（不是研究腳本簡化版）重新驗證——結果比研究腳本估計的更好：**6年裡5勝1負**（唯一輸2022空頭年，-1.51% vs +1.68%，差距不大），全期間286.64%→338.75%（比研究腳本估計的311.72%更高），MDD幾乎不變（25.93%→25.92%）。兩版落差提醒：研究腳本的簡化實作跟正式引擎終究有微幅差距，最終決策以正式引擎為準。

**已正式寫入`strategy_core.py`**：4個原本A/B/C/D/E共用的常數（鎖利觸發12%→14.93%、鎖利後ATR倍數1.25→1.1292、固定停利15%→18.72%）改成依`strategy=='E'`才生效的條件參數，A/B/C/D維持原值完全不受影響（已驗證方案D改動後MDD 27.36%與修改前記錄完全吻合）；E專屬的雙賣收緊ATR倍數（1.0→1.6226）本來就是E分支內的獨立邏輯，直接改。進場ATR停損倍數(3.0→2.99)差異在雜訊範圍內，維持3.0不變。`order_planner.html`下單建議頁預設的方案E，現在就是採用這組新參數。

**⑤首頁持股主動提醒（已完成）**：`index.html`新增`holdingsSellSignalBanner`，`app.js`的`loadHoldingsSellSignalBanner()`在頁面載入時呼叫既有的`/api/portfolio/sell_check`（跟「我的操盤室」同一套邏輯），有觸發賣出訊號的持倉數>0就顯示紅色提示橫幅並連結到`history.html`。

**④選股因子模型**：仍未開始，需要先跟使用者討論範疇（技術面/籌碼面因子候選、驗證方法），不在這輪自動執行範圍內。

## 2026-08-13（續5）：個案⑰——方案A極端漲幅風控訊號重新驗證，原結論未能複現

使用者問「列出來的這些研究，對模型有無獲利率提升的可能」，逐項評估後排出優先順序：①選股因子模型（高潛力，全新維度）②A/B/C/D的Optuna參數搜尋（中潛力）③方案A極端漲幅風控重新驗證（中潛力但有不確定性，因為原始訊號早於個案⑬修正、樣本量偏薄）。使用者指示「把前面三項做完，且依次寫入研究報告中」，從最快驗證的③開始。

**個案⑰**：個案①（2026-08-10）曾發現「方案A + 距60日低點漲幅超過100%」表現轉差、方案D沒有這個問題，並提議設計A方案專屬軟性風控。用目前（修正未來函數後）資料庫的top 4組A/D正式實驗重新驗證（2,689筆交易），**結果完全反轉**：>100%漲幅分桶現在是方案A表現最好的（勝率53.0%/平均報酬+4.81%，四個分桶裡最高），跟方案D已無明顯分歧，且兩方案在四個分桶都呈現單調遞增（漲幅越大表現越好）——這其實比原始發現更符合個案①自己引用的動能延續文獻。**結論：不建議繼續設計方案A的極端漲幅軟性風控規則，原始提案的前提訊號沒有通過重新驗證。**

**重要方法論教訓**：這是繼個案⑨（連買天數）之後，第二次發現「未來函數修正前的訊號分析」在修正後反轉。「距60日低點漲幅」本身是純價格訊號、不直接用籌碼資料，但因為進場篩選邏輯（哪些股票在何時被選中）用了籌碼資料，修正後連帶改變了整個交易樣本組成——**任何分析對象是「籌碼邏輯篩選出的交易樣本」的研究，即使分析的特徵本身不是籌碼指標，一樣可能被未來函數間接污染，都應視為未驗證，需要重新確認**。

**已完成的頁面更新**：`case_studies.html`新增個案⑰；`research_data/`新增`case17_strategyA_runup_revalidation.csv`，`README.md`同步更新。

## 2026-08-13（續6）：個案⑱——Optuna參數搜尋擴大到方案A/B/C/D，B/C高信心採用，A/D標記空頭年取捨

延續個案⑯（方案E的Optuna搜尋是這一天唯一乾淨成功的優化），把完全相同的方法論（訓練期2021-01-01~2024-11-01搜尋、測試期2024-11-02~2026-08-10樣本外驗證、正式`run_backtest`引擎逐年獨立回測複核）套用到方案A/B/C/D。範圍限制跟個案⑯一致：只搜尋`evaluate_exit()`內部的出場常數（鎖利觸發門檻、鎖利後ATR倍數、固定停利%，C/D另有籌碼轉弱放寬停損的門檻/倍數，A另有籌碼轉弱確認天數），不觸碰`initial_atr_mult`（進場ATR停損倍數）——這個值在`backtest_engine.py`、`main.py`的賣出訊號回放、`main.py`實際下單三處各自獨立設定，方案D還有regime相依邏輯（多頭2.2／非多頭1.5），這個既有不一致性刻意維持原樣、只當固定基準線。

**自我發現並修正的方法論錯誤**：第一版搜尋腳本把方案D的基準線也寫死成3.0（跟A/B/C/E共用），但正式引擎裡D其實有regime相依邏輯，用錯基準線讓「改善幅度」被誤放大（135.59%→400.98%看起來像超大改善）。自行發現後改用正確的regime相依基準線重跑，改善幅度回落到更合理的水準（169.91%→319.03%）。

**正式引擎逐年獨立回測結果**（8檔/30天，`is_grid_trial=True`）：4個方案全期間報酬與MDD都是新參數勝出，但逐年勝負分布差異很大——**方案B、D乾淨多數勝（5/6年）**；**方案C接近打平（3/6年）但2022空頭年有一次很大改善**（-27.31%→-15.97%）；**方案A逐年最弱（只贏2/6年）**。更關鍵的是，**方案A、D都在2022空頭年（唯一的完整空頭樣本）表現變差**（A：-9.26%→-18.29%；D：-12.38%→-17.42%），而B、C反而在同一年變好。

**結論與採用決定**：4個方案的新參數都已寫入`strategy_core.py`。**方案B、C以高信心採用**——逐年勝負乾淨且2022空頭年同步改善，報酬與防守同時變好。**方案A、D標記一項已知取捨後採用**：全期間報酬與MDD綜合指標都改善，但下一次真正空頭市場來臨時防守力可能不如舊參數——這跟個案⑯方案E在2021/2024表現變差但仍值得採用的取捨性質類似，但這次犧牲落在真正的空頭年，風險意義不完全一樣，後續應持續觀察而非視為定論。

**已完成的頁面更新**：`case_studies.html`新增個案⑱；`research_data/`新增`case18_optuna_ABCD_search_results.json`、`case18_ABCD_old_baseline_official_verification.csv`、`case18_ABCD_new_params_official_verification.csv`，`README.md`同步更新。`strategy_discussion.html`「後續討論串」項目③已更新為個案⑱的成果（原本只反映個案⑯的E專屬結果）。

## 2026-08-13（續7）：個案⑲——選股因子模型Optuna搜尋，負面結果，維持現行排序邏輯

「把前面三項做完」的第三項。比照個案⑯⑱的方法論，把現行選股排序公式（`chip_score×100+momentum`，兩因子固定權重）改成6個候選因子（籌碼積分、動能、ATR/收盤價相對波動度、量能強度、連續同買天數、60日買超金額百分位）的Optuna可搜尋加權組合，訓練期(2021-01-01~2024-11-01)搜尋60個trial，測試期(2024-11-02~2026-08-10)驗證。進場門檻(`chip_score≥1`)與出場邏輯(方案E)完全不變，只改「候選股票裡選誰」的排序方式。

**結果是明確的負面發現**：Optuna找到的「最佳」權重組合，連訓練期（用來搜尋的資料本身）都沒贏過現行排序公式（評分公式總報酬-2×MDD：現行97.73分 vs 搜尋到的最佳91.38分），測試期落差更大——總報酬110.94%→51.71%，接近腰斬。這跟個案⑯⑱（新參數訓練期、測試期都同步改善）的模式完全相反，是60個trial連訓練期都找不到出路，不是過度配適。新增的4個因子在最佳解裡權重全部是負值且量級偏小，代表這幾個籌碼衍生因子對「選股排序該選誰」沒有額外的預測力——這跟個案⑨（連買天數不該當硬性濾網）、個案⑩（買超金額大小不該加強）兩個舊發現的方向一致，只是這次改用加權排序的方式再驗證一次，結論相同。

**結論**：不建議改動現行的選股排序邏輯。這是誠實記錄的負面結果——現有免費資料源在「怎麼排序候選股票」這件事上，可能已經沒有太多額外可榨取的訊號。下一步若要真的提升選股，方向可能要轉向`strategy_discussion.html`已提過的「加入基本面資料」（本益比、EPS成長率等，需另外評估付費資料源），而不是繼續在現有免費資料的排列組合裡打轉。

**已完成的頁面更新**：`case_studies.html`新增個案⑲；`research_data/`新增`case19_factor_model_optuna_search.json`，`README.md`同步更新；`strategy_discussion.html`「後續討論串」項目④已更新為負面結果。至此使用者指示的「前三項研究」（選股因子模型、A/B/C/D Optuna搜尋、方案A風控重新驗證）全部完成並依序寫入研究報告。
